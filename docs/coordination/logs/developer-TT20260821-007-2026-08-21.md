# 开发工作日志 developer-TT20260821-007

- 任务号：T20260821-007（任务书 docs/coordination/tasks/T20260821-007.md）
- 开发者：developer@developer-01（身份已由规划 Agent 预注册为 developer-007，不重复注册）
- 日期：2026-08-21
- 状态：dev-ready

## 一、变更文件清单与 diff 摘要

1. `tests/test_auth.py`（git diff：+55/-14 区域内 69 行改动）
   - 新增工具函数 `logout(token=None)`：`client.post("/api/auth/logout", headers=auth_header(token))`，无 token 时不带 Authorization。
   - 7.3 重写为硬断言：重新登录取得新 token → `POST /api/auth/logout` 断言 200 且 body `== {"message": "登出成功"}` → 同一 token 调 `GET /api/auth/me` 断言 401 且 detail `== "未登录或登录已过期，请重新登录"`。
   - 7.4 重写为硬断言：登出前 `/me` 200 → `logout` 200 + `{"message": "登出成功"}` → 登出后同一 token `/me` 401（token 立即失效）。
   - 7.4b 新增幂等用例：无 token / 无效 token 调 logout 均返回 200 + `{"message": "登出成功"}`。
   - 删除「7.3 服务端缺少 /api/auth/logout」「7.4 旧Token仍有效（服务端未清除）」错误/误导文案。

2. `docs/INTERFACE.md`（+15 行）
   - 「## 规则」前新增「## HTTP API（logout）」契约：`POST /api/auth/logout`（可带 Bearer token，无请求体）；
     带有效 token → 200 `{"message": "登出成功"}` 并删除服务端 session；无/无效 token 幂等 200 不抛错；
     登出后同一 token 调 `GET /api/auth/me` 返回 401「未登录或登录已过期，请重新登录」。

## 二、契约逐条对照（任务书「一、可测试验收标准」）

1. 7.3 硬断言 login→logout 200+{"message":"登出成功"}→同一 token /me 401 —— ✅ 已实现（含 detail 精确断言）。
2. 7.4「服务端登出后 token 立即失效」：登出前 /me 200 → 登出 → /me 401；并补无 token 幂等 200 —— ✅ 已实现（另覆盖无效 token 幂等）。
3. 不再出现「当前系统没有服务端登出 API」「登出后旧 token 仍有效」等文案 —— ✅ Select-String 无命中。
4. INTERFACE.md 新增 POST /api/auth/logout 契约 —— ✅ 已新增（内容与任务书第三节一致）。
5. pytest test_auth 全绿；run_regression core/full 全绿；scan_secrets 0 命中 —— ✅ 见第三节输出。

范围核对：仅改 `tests/test_auth.py` 与 `docs/INTERFACE.md`；未改 main.py / auth.py / 前端 / 其它 tests / scripts / README / deploy。

## 三、验收命令输出摘要

1. `.venv\Scripts\python.exe -m pytest tests/test_auth.py -q` → `1 passed in 2.70s` / exit 0。
2. `Select-String -LiteralPath tests\test_auth.py -Pattern '服务端缺少|旧Token仍有效'` → 无命中；
   `docs\INTERFACE.md` 含「## HTTP API（logout）」与「POST /api/auth/logout」。
3. `.venv\Scripts\python.exe tests/run_regression.py core` → 11 通过 / 0 失败 / 残留备份检查=无 / exit 0（52.1s）。
4. `.venv\Scripts\python.exe tests/run_regression.py full` → 20 通过 / 0 失败 / 残留备份检查=无 / exit 0（154.7s）。
5. `.venv\Scripts\python.exe scripts/scan_secrets.py` → `SECRET_SCAN_TOTAL=115 / NO SECRET HITS` + .env gitignore 命中 / exit 0。

备注：首轮 core 回归中 `test_security_authorization.py` 曾出现「当前定位不在小区范围内，无法注册」失败，系并发子代理同时跑测试
（data/ 目录被其备份占位、community_config 瞬态）所致；待并发进程结束后单独复跑该文件 `8 passed`，再复跑 core 全绿。
本任务未触碰该文件与 geo/community 逻辑，与本任务改动无关。

## 四、流程说明

- 已向 `docs/coordination/inbox/TT20260821-007-planner.md` 写入【dev-ready】消息（统一消息头）。
- 未执行 git add/commit/push；未修改任务书范围外文件。