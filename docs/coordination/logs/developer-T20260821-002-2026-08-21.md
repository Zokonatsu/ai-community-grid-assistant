# developer 工作日志 · T20260821-002（CORS 收紧为环境变量白名单）

- 【消息类型】task-card
- 【任务号】T20260821-002
- 【版本】1
- 【发送方】planner@01a0230e-cc29-7490-bddb-9d1ae703876a
- 【接收方】developer@developer-01
- 【时间】2026-08-21T15:00:00+08:00
- 任务书：docs/coordination/tasks/T20260821-002.md（对应问题清单项：问题 2 CORS 全开）
- 身份：developer-02 已由规划 Agent 预注册，未重复追加 agent-registry.md。

## 一、变更文件清单 + diff 摘要

### 1. `config.py`（+16）
- 新增 CORS 小节与 `CORS_ALLOW_ORIGINS` 解析（列表推导）：
  - 逗号分隔、允许空格、逐项 trim、空项忽略；
  - 未设置时默认 `http://127.0.0.1:8000,http://localhost:8000,http://118.31.58.191:8000`；
  - 结果为 `list[str]`，main.py 直接消费。

### 2. `main.py`（+16/-4）
- CORSMiddleware 段由 `allow_origins=["*"]` + `allow_credentials=True` 改为：
  - `allow_origins=config.CORS_ALLOW_ORIGINS`（环境变量白名单）；
  - `_cors_allow_credentials = "*" not in _cors_allow_origins`（列表含 * 时 False，否则 True）；
  - 含 * 时 `logger.warning(...)` 记录降级提示（建议显式域名白名单）；
  - `allow_methods` / `allow_headers` 保持 `["*"]`。

### 3. `.env.example`（+9）
- 末尾新增「CORS 跨域来源白名单」小节：变量说明、默认值（含生产前端
  http://118.31.58.191:8000）、追加示例、* 通配时 allow_credentials 降级的安全提示。

### 4. `README.md`（本任务 +9 行）
- 「3. 配置环境变量」env 示例块新增 CORS_ALLOW_ORIGINS 两行；
- 新增「CORS 说明」引用块：默认含本机 + 生产前端 http://118.31.58.191:8000、改 .env 即可、
  * 时 allow_credentials 降级为 false。
（注：该文件另有并行子代理 T20260821-006 的交叉引用改动，git diff 计数含两者。）

### 5. `deploy/DEPLOY.md`（本任务 +10 行）
- 「3.2 配置环境变量」env 示例块新增 CORS_ALLOW_ORIGINS 两行；
- 「7.1 发布前检查清单」新增第 5 项：核对 CORS_ALLOW_ORIGINS 白名单
  （确认含生产前端域名且不含 *）。
（注：该文件另有并行子代理 T20260821-006 的第 7/8 章改动，git diff 计数含两者。）

### 6. `tests/test_cors.py`（新增，5 个用例）
- test_default_allows_local_and_production_origins：默认放行 127.0.0.1:8000 / localhost:8000 /
  118.31.58.191:8000 且 ACAO 回显该 origin；
- test_default_rejects_evil_origin：evil.example.com 无 ACAO 头；
- test_env_override_replaces_default_whitelist：自定义 a/b 放行、旧默认 118.31.58.191 不再放行；
- test_wildcard_disables_credentials_and_logs_warning：* 时响应无凭据头 + warning 日志；
- test_default_source_has_no_wildcard_allow_origins：main.py 源码无 `allow_origins=["*"]`。
- 实现要点：config/main 在导入期读环境变量，故按用例 importlib.reload(config)+reload(main)
  重建 app/TestClient；并 patch receive_agent.OpenAI（与 test_security_authorization 同模式），不触网。

`git diff --stat -- config.py main.py .env.example README.md deploy/DEPLOY.md tests/test_cors.py`：

```
 .env.example     |   9 +++
 README.md        |  11 ++++
 config.py        |  16 ++++++
 deploy/DEPLOY.md | 164 ++++++++++++++++++++++++++++++++++++++++++++++++++++++-
 main.py          |  16 +++++-
 5 files changed, 212 insertions(+), 4 deletions(-)
```
（test_cors.py 为新增未跟踪文件，不计入 diff --stat；README/DEPLOY.md 计数含并行改动。）

## 二、冻结契约逐条对照

1. `CORS_ALLOW_ORIGINS` 逗号分隔/允许空格/逐项 trim/空项忽略；未设置默认三来源：
   ✔ 满足（config.py；实测 trim/空项：`" https://a.example.com , ,https://b.example.com, "` → 两元素）。
2. main.py allow_origins 取自 config 列表；列表含 * → allow_credentials=False + warning；
   否则 True；allow_methods/allow_headers 保持 ["*"]：✔ 满足。
3. 响应行为：放行 origin → ACAO 回显该 origin；未放行 → 无 ACAO 头：✔ 满足（test_cors.py 覆盖）。
4. 默认配置下源码无 `allow_origins=["*"]`：✔ 满足（Select-String 0 命中）。
5. .env.example / README / DEPLOY.md 记录变量与生产前端域名 http://118.31.58.191:8000；
   DEPLOY.md 发布前检查清单同步：✔ 满足。
6. 新增 tests/test_cors.py 覆盖 1-3；run_regression core/full 全绿：✔ 满足。

## 三、验收命令输出摘要

1. `.venv\Scripts\python.exe -m pytest tests/test_cors.py -q`：`5 passed in 2.98s`，exit 0。
2. `.venv\Scripts\python.exe tests/run_regression.py core`：11 通过 / 0 失败 / 总耗时 51.0s /
   残留备份检查=无 / exit 0。
3. `.venv\Scripts\python.exe tests/run_regression.py full`：21 通过 / 0 失败 / 总耗时 152.5s /
   残留备份检查=无 / exit 0（含 test_cors.py PASS 4.8s）。
4. `.venv\Scripts\python.exe scripts/scan_secrets.py`：SECRET_SCAN_TOTAL=121 / NO SECRET HITS，
   exit 0（.env gitignore 校验 PASS）。
5. `Select-String main.py 'allow_origins=["*"]'`：NO_MATCH（默认无 allow_origins=["*"]）。

## 四、范围说明
- 未触碰禁区：deploy/Dockerfile、tests/test_auth.py、docs/INTERFACE.md、docs/coordination/tasks/、
  scripts/、record_agent.py、secure_store.py、workflow.py、receive_agent.py、dispatch_agent.py、
  data/、secure/、deploy/docker-compose.yml。
- 未执行 git add/commit/push。
- 全部改动文件 UTF-8 无 BOM、CRLF 行尾（与仓库一致）。
