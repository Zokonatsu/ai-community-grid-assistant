# developer 工作日志 · T20260821-006（回滚 SOP 与扩容说明文档）

- 【消息类型】task-card
- 【任务号】T20260821-006
- 【版本】1
- 【发送方】planner@01a0230e-cc29-7490-bddb-9d1ae703876a
- 【接收方】developer@developer-01
- 【时间】2026-08-21T15:00:00+08:00
- 任务书：docs/coordination/tasks/T20260821-006.md（对应问题清单项：问题 7 无回滚与扩容文档）
- 身份：developer-006 已由规划 Agent 预注册，未重复追加 agent-registry.md。

## 一、变更文件清单 + diff 摘要

仅 2 个文件（纯文档，零代码/测试/接口改动）：

### 1. `deploy/DEPLOY.md`（+159 / -1）

- 第 1 章高可用提示行加交叉引用：
  `> 生产环境如需高可用，建议搭配负载均衡并横向扩展（约束与方案见第 8 章「扩容与多实例」）。`
- 在第 6 章「安全注意事项」之后、附录之前新增两章：
  - `## 7. 标准化回滚 SOP`：7.1 发布前检查清单（备份 data/、secure/、.env 与 DATA_ENCRYPTION_KEY、记录当前镜像 tag）｜7.2 回滚步骤（docker compose down → 恢复备份 → 上一版本镜像 up → curl /health 验证 → 抽查数据可解密）｜7.3 回滚决策与验证标准｜7.4 注意事项（events.jsonl 追加不回滚、tasks.json 以备份为准、密钥与加密文件必须成对恢复）。
  - `## 8. 扩容与多实例`：8.1 单实例约束说明（内存 _tasks、本地写锁、secure/ 本地加密文件、events.jsonl 追加并发）｜8.2 方案 A 单机升配 + AUTH_STORE=cloudbase 账号上云｜8.3 方案 B 多副本需共享 data 卷或对象存储 + events.jsonl 多进程追加并发风险与建议｜8.4 Nginx 多 upstream 示例。
- 文件保持 UTF-8 无 BOM、CRLF 行尾（与仓库工作区一致）。

### 2. `README.md`（+4）

- 「数据持久化」小节末尾新增交叉引用：
  `> **生产部署**：事件的备份/回滚、扩容与多实例的写入约束，详见 deploy/DEPLOY.md 第 7 章「标准化回滚 SOP」与第 8 章「扩容与多实例」。`
- 「账号数据加密存储」小节末尾新增交叉引用：
  `> **生产部署**：DATA_ENCRYPTION_KEY 与 secure/*.enc 必须成对备份/恢复（回滚 SOP 见 deploy/DEPLOY.md 第 7 章「标准化回滚 SOP」）；多副本扩容时账号数据建议 AUTH_STORE=cloudbase 上云（见 deploy/DEPLOY.md 第 8 章「扩容与多实例」）。`
- 文件保持 UTF-8 无 BOM、CRLF 行尾。

`git diff --stat -- README.md deploy/DEPLOY.md`：

```
 README.md        |   4 ++
 deploy/DEPLOY.md | 160 ++++++++++++++++++++++++++++++++++++++++++++++++++++++-
 2 files changed, 163 insertions(+), 1 deletion(-)
```

说明：全量 `git diff --stat` 另含 deploy/Dockerfile、docs/INTERFACE.md、docs/coordination/agent-registry.md、tests/test_auth.py，均为并行子代理/规划 Agent 改动（开工前或并行期间已存在），非本任务改动；本任务只改上述 2 个文档。

## 二、冻结契约逐条对照

1. 验收标准 1「DEPLOY.md 回滚 SOP」：✅ 覆盖全部要点——发布前检查清单（备份 data/、secure/、.env 与 DATA_ENCRYPTION_KEY、记录当前镜像 tag，7.1）；回滚步骤（docker compose down → 恢复备份 → 上一版本镜像 up → curl /health 验证 → 抽查数据可解密，7.2）；回滚决策与验证标准（7.3）；注意事项（events.jsonl 追加不回滚、tasks.json 以备份为准、密钥与加密文件必须成对恢复，7.4）。
2. 验收标准 2「DEPLOY.md 扩容与多实例」：✅ 覆盖全部要点——单实例约束说明（内存 _tasks、本地写锁、secure/ 本地加密文件、events.jsonl 追加并发，8.1）；方案 A 单机升配 + AUTH_STORE=cloudbase 账号上云（8.2）；方案 B 多副本需共享 data 卷或对象存储 + events.jsonl 多进程追加并发风险与建议（8.3）；Nginx 多 upstream 示例（8.4）。
3. 验收标准 3「README 交叉引用」：✅ 数据持久化、账号数据加密存储两处交叉引用第 7/8 章。
4. 验收标准 4「改动范围 + 回归」：✅ 仅 deploy/DEPLOY.md 与 README.md；无代码/测试/接口改动；run_regression core 全绿（11/11）。

## 三、验收命令输出摘要

1. `Select-String -Path deploy\DEPLOY.md -Pattern '回滚|扩容|多实例'`：命中 ≥20 处（第 7/8 章全部小节标题与要点），且含「发布前检查清单」（L336）、「回滚步骤」（L349）、「验证命令」（L382）。
2. `Select-String -Path README.md -Pattern '回滚|扩容'`：2 处命中（L354、L372，交叉引用）。
3. `git diff --stat`：本任务改动仅 README.md（+4）与 deploy/DEPLOY.md（+159/-1）；其余文件为并行改动（见一、说明）。
4. `.venv\Scripts\python.exe tests\run_regression.py core`：11/11 PASS / 0 失败 / 总耗时 44.2s / 退出码 0（残留备份检查=无）。
