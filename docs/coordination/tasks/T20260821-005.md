# 任务书 T20260821-005：Prometheus 监控指标接入 + 告警规则文档

## 身份头
- from: planner@01a0230e-cc29-7490-bddb-9d1ae703876a
- to: developer@developer-01
- task-id: T20260821-005
- 版本: 1
- 时间: 2026-08-21T15:00:00+08:00
- 对应问题清单项：问题 5（无监控与告警）
- 派发队列：001→002→003→004→005→006→007

## 一、需求（已确认，用户选 A：Prometheus 方案）
**一句话目标**：接入 Prometheus 指标端点 `GET /metrics`（prometheus-fastapi-instrumentator），并输出 Prometheus + Alertmanager 采集与告警规则文档，为单机部署提供可观测性基线。

**已核实的现状**：全仓无 prometheus/sentry/metrics 实现。

**可测试验收标准**
1. `GET /metrics`（无登录）返回 200 + `text/plain`，且包含 `http_requests_total`、`http_request_duration_seconds` 等标准指标。
2. 可观测性冒烟：连续访问 `GET /health` 2 次后，`http_requests_total` 中对应计数增加（指标可观测变化）。
3. `/metrics` 不需要 Authorization（无 token 也 200）。
4. `docs/监控告警.md` 存在且包含：指标清单、Prometheus scrape 配置示例（job/端口/interval）、≥3 条 Alertmanager 告警规则示例（如 5xx 比例、接口 P95 耗时、实例存活、事件处理失败率）；`deploy/DEPLOY.md` 新增「监控与告警」章节引用该文档。
5. run_regression core/full 全绿；scan_secrets 0 命中。

## 二、范围
- 修改文件：`requirements.txt`（+prometheus-fastapi-instrumentator，锁定版本）、`main.py`（Instrumentator 挂载）、`docs/监控告警.md`（新建）、`deploy/DEPLOY.md`（新增章节）、`tests/`（/metrics 冒烟，可并入 test_server 或新文件）、`docs/INTERFACE.md`（可选增补 /metrics 契约，若增补则冻结）。
- 明确不做：不部署真实 Prometheus/Alertmanager 实例（本任务产出端点+文档）；不做 Sentry；不改业务接口字段。

## 三、冻结契约
- `GET /metrics`：无需登录，200，Content-Type `text/plain; version=0.0.4; charset=utf-8`，含 `http_requests_total` 与 `http_request_duration_seconds` 指标（Instrumentator 默认标签）。
- 指标端点不参与业务限流（RATE_LIMIT_ENABLED 不影响 /metrics；如限流中间件按路径过滤，/metrics 豁免）。
- 文档必须含：指标清单表、scrape 配置示例、≥3 条告警规则示例（yaml）、指标端点安全建议（如反向代理内网限制访问）。

## 四、验收命令
1. `python -m pytest tests/<metrics 冒烟测试> -q` 全绿（200 + 指标名 + 计数变化 + 无鉴权）。
2. `python tests/run_regression.py core` 与 `full` 全绿。
3. `python scripts/scan_secrets.py` 0 命中。
4. `Select-String -Path docs\监控告警.md -Pattern 'http_requests_total|scrape|alert'` 命中。

## 五、产出要求
- 变更清单：requirements、main.py、文档、测试 diff 摘要。
- 测试结果：上述验收命令输出摘要。
- 以【dev-ready】消息回复 from（planner 会话）。