# 任务书 T20260821-006：回滚 SOP 与扩容说明文档

## 身份头
- from: planner@01a0230e-cc29-7490-bddb-9d1ae703876a
- to: developer@developer-01
- task-id: T20260821-006
- 版本: 1
- 时间: 2026-08-21T15:00:00+08:00
- 对应问题清单项：问题 7（无回滚与扩容文档）
- 派发队列：001→002→003→004→005→006→007

## 一、需求（已确认）
**一句话目标**：在 `deploy/DEPLOY.md` 补充「标准化回滚 SOP」与「扩容与多实例」两个文档章节，让运维在上线失败时可按清单回滚、在需要扩容时知道约束与方案。

**已核实的现状**：DEPLOY.md 共 364 行，已有 5.3 更新版本、5.5 备份数据、6 安全注意事项，但无回滚 SOP 与扩容章节；数据形态：data/（明文/加密事件任务）、secure/*.enc（加密账号，需 DATA_ENCRYPTION_KEY）、AUTH_STORE=file|cloudbase。

**可测试验收标准**
1. DEPLOY.md 新增「回滚 SOP」章节：含发布前检查清单（备份 data/、secure/、.env 与 DATA_ENCRYPTION_KEY、记录当前镜像 tag）、回滚步骤（docker compose down → 恢复备份 → 上一版本镜像 up → curl /health 验证 → 抽查数据可解密）、回滚决策与验证标准、注意事项（events.jsonl 追加不回滚、tasks.json 以备份为准、密钥与加密文件必须成对恢复）。
2. DEPLOY.md 新增「扩容与多实例」章节：单实例约束说明（内存 _tasks、本地写锁、secure/ 本地加密文件、events.jsonl 追加并发）；扩容方案 A（单机升配 + AUTH_STORE=cloudbase 账号上云）；方案 B（多副本需共享 data 卷或对象存储，并明确 events.jsonl 多进程追加并发风险与建议）；Nginx 多 upstream 示例（可选）。
3. `README.md` 在数据持久化/部署相关位置交叉引用上述两个章节。
4. 文档改动仅限 deploy/DEPLOY.md 与 README.md；无代码/测试/接口改动；run_regression core 全绿（回归确认）。

## 二、范围
- 修改文件：`deploy/DEPLOY.md`、`README.md`。
- 明确不做：不改任何代码/测试/接口；不新增部署/回滚脚本（纯文档）；不改 docker-compose.yml。

## 三、冻结契约
- 无接口变更（纯文档任务）。文档章节标题建议：`## 6.5 标准化回滚 SOP`（或独立 `## 7 回滚 SOP`）与 `## 8 扩容与多实例`；内容必须覆盖验收标准 1-2 的全部要点。

## 四、验收命令
1. `Select-String -Path deploy\DEPLOY.md -Pattern '回滚|扩容|多实例'` 命中且含检查清单/步骤/验证命令。
2. `Select-String -Path README.md -Pattern '回滚|扩容'` 命中（交叉引用）。
3. `git diff --stat` 仅 deploy/DEPLOY.md 与 README.md。
4. `python tests/run_regression.py core` 全绿（回归确认）。

## 五、产出要求
- 变更清单：DEPLOY.md / README.md 新增章节 diff 摘要。
- 测试结果：上述验收命令输出摘要。
- 以【dev-ready】消息回复 from（planner 会话）。