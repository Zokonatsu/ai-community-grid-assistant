# 任务书 T20260821-001：生产镜像基础镜像修正（Dockerfile）

## 身份头
- from: planner@01a0230e-cc29-7490-bddb-9d1ae703876a
- to: developer@developer-01
- task-id: T20260821-001
- 版本: 1
- 时间: 2026-08-21T15:00:00+08:00
- 对应问题清单项：问题 1（生产镜像不可构建）
- 派发队列：001→002→003→004→005→006→007，按序处理，前一任务 accepted 后再处理下一项

## 一、需求（已确认）
**一句话目标**：修正 `deploy/Dockerfile` 使用不存在的 `python:3.14-slim` 基础镜像，改为本环境与 CI 一致的 `python:3.12-slim`，使生产镜像可构建。

**已核实的现状**
- `deploy/Dockerfile` 第 1-2 行：`# 基于 Python 3.14 官方精简镜像` + `FROM python:3.14-slim`。
- 本机 `.venv` Python 版本 3.12.7；CI（`.github/workflows/ci.yml`）setup-python 3.12；全仓仅 deploy/Dockerfile 引用 3.14。

**可测试验收标准**
1. `deploy/Dockerfile` 首行为 `FROM python:3.12-slim`，上方注释同步改为 Python 3.12。
2. 全仓 grep `3.14` 零命中（deploy/ 与根目录）。
3. 除 Dockerfile 第 1-2 行外无任何代码/测试/文档改动（本任务只允许这两行）。
4. 本机无 docker，不强制 docker build；如执行环境可用 docker，则 `docker build -f deploy/Dockerfile -t app-test .` 需成功（可选验收项）。
5. 回归确认：`python tests/run_regression.py core` 全绿。

## 二、范围
- 修改文件：`deploy/Dockerfile`（仅第 1-2 行：注释 + FROM）。
- 明确不做：不改 requirements.txt、不建 CI docker 构建 job、不动其它文件。

## 三、冻结契约
- 无 HTTP 接口变更（部署层修改）。
- 镜像基础：`python:3.12-slim`（与本地 venv 3.12.7、CI setup-python 3.12 保持一致）；`CMD`、`EXPOSE`、`ENV TZ` 等其余行保持不动。

## 四、验收命令
1. `Select-String -Path deploy\Dockerfile -Pattern 'FROM python:3.12-slim'` 命中且无 `3.14`。
2. 全仓 grep `3.14` 无命中。
3. `git diff --stat deploy/Dockerfile` 仅 1 个文件。
4. `python tests/run_regression.py core` 全绿（回归确认）。
5. `python scripts/scan_secrets.py` 0 命中。

## 五、产出要求
- 变更清单：deploy/Dockerfile 两行 diff。
- 测试结果：上述验收命令输出摘要。
- 以【dev-ready】消息回复本任务书身份头中的 from（planner 会话）。