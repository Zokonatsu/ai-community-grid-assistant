# 任务书 T20260821-007：修正 test_auth 登出断言与 INTERFACE 契约

## 身份头
- from: planner@01a0230e-cc29-7490-bddb-9d1ae703876a
- to: developer@developer-01
- task-id: T20260821-007
- 版本: 1
- 时间: 2026-08-21T15:00:00+08:00
- 对应问题清单项：问题 8（测试脚本与代码事实不一致）
- 派发队列：001→002→003→004→005→006→007

## 一、需求（已确认）
**一句话目标**：修正 `tests/test_auth.py` 553-559 行「7.3 服务端缺少 /api/auth/logout」的错误断言与「7.4 登出后旧 token 仍有效」的误导性描述，并补 INTERFACE.md 的登出契约。

**已核实的事实基线**
- `main.py:523` 已实现 `POST /api/auth/logout`：解析 Authorization Bearer → `auth.logout_user(token)` → 返回 `{"message": "登出成功"}`；无/无效 token 时幂等返回 200。
- `auth.py:449 logout_user(token)`：从服务端 session 存储删除该 token 并落盘，使其立即失效。
- `tests/test_auth.py:552-559`「7.3 服务端缺少 /api/auth/logout」声称当前系统没有服务端登出 API、建议添加端点——与事实不符；`561-570`「7.4 旧Token仍有效（服务端未清除）」未真正调用 logout 却描述「登出后服务端 session 仍存在」，误导。
- `docs/INTERFACE.md`（88 行）未收录 logout 端点。

**可测试验收标准**
1. `tests/test_auth.py` 7.3 改为硬断言：登录 → 携带 token 调 `POST /api/auth/logout` → 200 且 body `{"message": "登出成功"}`；随后同一 token 调 `GET /api/auth/me` → 401「未登录或登录已过期，请重新登录」。
2. 7.4 改为「服务端登出后 token 立即失效」：登出前 /me 200 → 登出 → /me 401；并补用例：无 token 调 logout → 200（幂等）。
3. 测试文件中不再出现「当前系统没有服务端登出 API」「登出后旧 token 仍有效」等错误文案。
4. `docs/INTERFACE.md` 新增 `POST /api/auth/logout` 契约（见第三节）。
5. `python -m pytest tests/test_auth.py -q` 全绿；run_regression core/full 全绿；scan_secrets 0 命中。

## 二、范围
- 修改文件：`tests/test_auth.py`（7.3/7.4 重写）、`docs/INTERFACE.md`（新增 logout 契约）。
- 明确不做：不改 `main.py`/`auth.py`（logout 已实现，仅测试与契约文档修正）；不改前端。

## 三、冻结契约（INTERFACE.md 新增）
`POST /api/auth/logout`（可带 Bearer token，无请求体）：
- 鉴权：可选（幂等端点）；带有效 token → 删除服务端 session。
- 成功：200，`{"message": "登出成功"}`。
- 幂等：无 token / 无效 token 同样返回 200 `{"message": "登出成功"}`（不抛错）。
- 行为：登出后该 token 立即失效，`GET /api/auth/me` 返回 401。

## 四、验收命令
1. `python -m pytest tests/test_auth.py -q` 全绿。
2. `python tests/run_regression.py core` 与 `full` 全绿。
3. `python scripts/scan_secrets.py` 0 命中。
4. `Select-String -LiteralPath tests\test_auth.py -Pattern '服务端缺少|旧Token仍有效'` 无命中；INTERFACE.md 含 logout 契约。

## 五、产出要求
- 变更清单：test_auth.py 7.3/7.4 diff 与 INTERFACE.md diff 摘要。
- 测试结果：上述验收命令输出摘要。
- 以【dev-ready】消息回复 from（planner 会话）。