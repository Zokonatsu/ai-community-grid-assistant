# 任务书 T20260820-001-TD：测试基建收尾（崩溃保护复核 / 警告清理 / 脚本定位说明 / 全量复核）

## 身份头
- from: planner@01a01a02-fd78-70e2-b6fc-97a86b7d684c
- to: developer@developer-01
- task-id: T20260820-001-TD
- 版本: 1
- 时间: 2026-08-20T02:23:34+08:00
- 上游: T20260820-001（测试方案）；T-A/T-B/T-C 已归档（本任务处理其验收中记录的 P3 观察项）

## 一、需求（已确认）
**一句话目标**：测试基建收尾——①复核并固化 conftest 数据隔离的崩溃保护（失败/中断不残留 _pytest_bak_*）；②清理已知测试警告（PytestReturnNotNoneWarning、协程未 await RuntimeWarning、httpx/starlette 弃用警告）；③明确 run_tests.py 与 test_server.py 的定位说明；④核心/full runner 与 CI 保持一致并全量复核全绿。

**可测试验收标准**
1. 崩溃保护强度测试：新增用例模拟「测试模块内抛异常」，断言结束后无 `_pytest_bak_*` / `*.bak.*` 残留，且 `data/`、`secure/` 恢复原状（内容与运行前一致）。
2. 警告清理：
   - `test_register_location.py::test_suite` 不再返回值（消除 PytestReturnNotNoneWarning），断言逻辑不变。
   - `test_event_cancel.py` 不再产生「coroutine was never awaited」RuntimeWarning（修复或抑制并说明原因）。
   - httpx/starlette 弃用警告：通过 `pytest.ini` `filterwarnings` 抑制并注释说明（不升级依赖，除非评估必要且安全）；或明确记录为依赖行为不处理。
3. 定位说明：`run_tests.py` 文件头注明「实时冒烟脚本（需服务在跑 + 有效 token），非回归套件」；README 注明其定位与 `test_server.py`（远程部署冒烟、本地默认 skip）。
4. 一致性：`tests/run_regression.py` 的 core/full 列表与 `ci.yml`（core）及 README 描述一致；无重复/遗漏脚本。
5. 全量复核：`python tests/run_regression.py full` 与 `core` 全绿、残留备份检查=无；新增强度用例纳入 full。
6. 安全/范围：密钥扫描 0 命中；业务代码零改动（git diff 仅 tests/、pytest.ini、run_tests.py、README、可选 conftest 最小扩展）。

## 二、范围
**新增文件**
- `tests/test_infra_strength.py`（入 FULL）：崩溃保护强度测试——子进程运行一个「故意失败」的探针测试模块，断言失败后无残留备份目录且 data/secure 原样。
- 探针模块 `tests/_infra_probe_fail.py`（不匹配 `test_*.py` 收集规则，仅供强度测试子进程显式调用；故意抛 AssertionError）。

**修改文件**
- `tests/test_register_location.py`：`test_suite` 末尾去掉返回值（`return 0/1` → 无返回/`return None`），断言逻辑不变。
- `tests/test_event_cancel.py`：消除协程未 await 的 RuntimeWarning（定位到 simulate_* 或 wait_for mock 处，正确 await/取消或按原因抑制并注释）。
- `pytest.ini`：`filterwarnings` 加入 StarletteDeprecationWarning 抑制（带注释说明依赖行为）；如需一并抑制其它已知无害警告须逐条注明。
- `tests/run_tests.py`（仅注释）：文件头 docstring 明确「实时冒烟脚本」定位，不改任何行为。
- `README.md`：新增/补充「run_tests.py 定位」与「test_server.py 远程冒烟说明」段落；更新警告处理说明（可选）。
- `tests/run_regression.py`：若需要，把新强度用例加入 FULL（CORE 不加，保证 CI core 快速稳定）。

**明确不做**
- 不改业务代码/接口/前端。
- 不改变现有测试断言语义（仅去返回值/修警告/加说明）。
- 不升级/降级 fastapi/httpx/starlette 依赖（除非评估显示必要且全量回归仍绿；默认用 filterwarnings 记录处理）。
- 不改 `run_tests.py` 与 `test_server.py` 的执行行为（仅定位说明）。
- 不做 D1/D2 业务缺陷修复（另立任务）。

## 三、冻结契约

### 3.1 崩溃保护强度测试（test_infra_strength.py）
- 实现方式：子进程运行 `python -m pytest tests/_infra_probe_fail.py -q`（探针模块内一个用例故意 `assert False`；conftest 的 module 级 autouse data_isolation 自动生效），断言子进程退出码非 0。
- 断言：子进程结束后（1）项目根目录无 `_pytest_bak_*` / `data.bak.*` / `secure.bak.*` 残留；（2）`data/`、`secure/` 快照（文件清单+SHA256）与运行前一致。
- 探针模块命名 `_infra_probe_fail.py`：不以 `test_` 开头，避免被常规收集；强度测试显式指定路径运行。
- 纳入 FULL；CORE 不含（保 CI core 快速稳定）。

### 3.2 警告清理
- test_register_location：`test_suite` 不返回值（删除 `return 0/1`，保留 `assert all_pass` 语义）；直跑入口仍按结果 sys.exit(0/1)。
- test_event_cancel：定位协程未 await 的根因（大概率在 simulate_* 用 `side_effect=asyncio.TimeoutError()` 使 wait_for 未等待内部 `to_thread` 协程），修复为等待/取消协程或抑制该特定警告并注释原因；不得用 `warnings.filterwarnings("ignore")` 全局吞掉。
- pytest.ini `filterwarnings`：`ignore:Using \`httpx\` with \`starlette.testclient\` is deprecated:starlette...StarletteDeprecationWarning`（按实际 warning 类别精确匹配），注释说明为依赖行为、暂不升级。

### 3.3 定位说明
- run_tests.py 文件头：明确「实时冒烟脚本：需服务已启动（默认 127.0.0.1:8000）且 TOKEN 有效；非 pytest 回归套件；回归请用 tests/run_regression.py core/full」。
- README：新增「run_tests.py 与 test_server.py 定位」小节：run_tests.py=实时冒烟（对运行中服务发请求）；test_server.py=远程部署主机（118.31.58.191）冒烟，本地默认 skip（pytestmark），不入 CI。

### 3.4 一致性与全量复核
- run_regression.py CORE/FULL 列表与 ci.yml（core）一致；强度测试入 FULL。
- 全量：`python tests/run_regression.py full` 与 `core` 全绿、残留=无。

## 四、验收命令
1. `python -m pytest tests/test_infra_strength.py -q` 通过（含失败探针断言）。
2. 警告清理验证：`python -m pytest tests/test_register_location.py tests/test_event_cancel.py -q` 运行无 PytestReturnNotNoneWarning / coroutine RuntimeWarning（可加 `-W error::pytest.PytestReturnNotNoneWarning` 严格验证）；全量 cov 运行输出中不再出现上述两类警告。
3. `python -m pytest tests --collect-only -q` 无收集错误（探针模块未被常规收集）。
4. `python tests/run_regression.py core` 与 `full` 全绿、残留=无。
5. README 含 run_tests.py / test_server.py 定位说明；run_tests.py 文件头有定位注释。
6. 密钥扫描 `python scripts/scan_secrets.py` 0 命中；git diff 范围检查（仅 tests/、pytest.ini、run_tests.py、README、可选 conftest 最小扩展；业务代码零改动）。

> ⚠️ 沿用约定：改前备份 data/secure；勿并行；跑前确保无 file 模式 8000 服务并发写。

## 五、产出要求
- 变更清单：新强度测试/探针 + 各修改文件 diff 摘要（含警告修复原因说明）。
- 测试结果：验收命令 1-6 输出摘要。
- 以【dev-ready】消息回复本任务书身份头中的 from（planner）会话。