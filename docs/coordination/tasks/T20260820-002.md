# 任务书 T20260820-002：D1/D2 事件链路业务缺陷修复

## 身份头
- from: planner@01a01a02-fd78-70e2-b6fc-97a86b7d684c
- to: developer@developer-01
- task-id: T20260820-002
- 版本: 1
- 时间: 2026-08-20T02:58:20+08:00
- 上游: T20260820-001-TB 验收发现项 D1/D2（tests/test_chain_breaks.py「现状记录」基线）

## 一、需求（已确认）
**一句话目标**：修复事件链路两处业务缺陷——D1：receive_node 返回 None 时 create_event 对 None 调用 .get() 抛 AttributeError 且响应回显内部异常；D2：_process_event 对 invoke 返回的缺字段/非 dict 结果未校验而置「已完成」（表面成功）。修复后对应测试由「现状记录」转为硬断言。

**已定位代码**
- D1：`main.py` create_event 语义校验段——`semantic_result = await asyncio.wait_for(asyncio.to_thread(receive_node, check_state), timeout=50.0)`（约 775-789）；try 仅包 wait_for；try 之后（约 902 起）直接 `semantic_result.get("event_type", "")` 等 15 处 .get()。receive_node 返回 None 时 → AttributeError 逸出 create_event，响应/任务回显内部异常类型与消息。
- D2：`main.py` `_process_event`（约 164-178）——`result = await asyncio.wait_for(asyncio.to_thread(_run), timeout=60.0)` 后先 `if task["status"] == "处理中": task["status"] = "已完成"` 再 `task.update({...result["address"], result["event_type"], result["urgency"], result["scene_tag"], result["handler"]...})`；result 缺字段/非 dict 时在「已置已完成」之后才抛错，且异常分支的状态守卫（仅处理中/待审核可改写）因状态已变为已完成而失效 → 「表面成功/状态不一致」。
- 测试基线：`tests/test_chain_breaks.py` —— 4.3（receive_node 返回 None，现状记录 D1）；2.4（invoke 返回缺字段 dict，现状记录 D2）。

**可测试验收标准**
1. D1：receive_node 返回 None（或非 dict）时，`POST /api/events` 返回 success=true + data.status="待审核" + 响应 error 为 None；任务 error 为通用文案「语义校验服务异常，已转人工审核」；响应/任务 error 均不含 AttributeError、异常类型、堆栈、路径、行号。
2. D2：invoke 返回缺字段 dict 或非 dict/None 时，任务置「处理失败」并落盘（tasks.json 可重载）；error 为明确文案 `事件处理结果缺失必需字段：<字段名,字段名>` 或 `事件处理结果无效`；不含 Traceback/路径/行号/异常类型前缀；不得出现「status=已完成 但数据不完整」的中间态。
3. 回归不破坏：既有「工具抛错→处理失败」「节点 KeyError→处理失败」「成功→已完成」「已撤销防覆盖」「待审核保留」等 _process_event 语义与 create_event 超时/API异常/无效输入降级全部保持不变。
4. 接口契约不变：HTTP 响应字段名/类型不变；INTERFACE.md 无需变更（仅行为收紧）。
5. 测试转换：test_chain_breaks.py 中 D1/D2 由「现状记录」改为「硬断言」。
6. 全量复核：run_regression full 与 core 全绿、残留=无；密钥扫描 0 命中；业务改动仅限 main.py 与 test_chain_breaks.py。

## 二、范围
**修改文件**
- `main.py`：create_event 语义段后新增 None/类型守卫（D1）；`_process_event` 重构校验顺序（D2）。
- `tests/test_chain_breaks.py`：4.3 与 2.4 由「现状记录」转「硬断言」，并补非 dict（None）返回的 D2 断言。

**明确不做**
- 不改接口契约字段/类型、不改 INTERFACE.md。
- 不改 receive_agent.py / dispatch_agent.py / record_agent.py 业务逻辑（仅 main.py 消费侧防御）。
- 不改其它测试文件语义；不改前端。
- 不做真实 LLM/网络验证（离线 mock）。

## 三、冻结契约

### 3.1 D1 修复（main.py create_event）
- 位置：语义 try/except 块结束后、`event_type = semantic_result.get(...)` 之前。
- 逻辑：
  ```
  if semantic_result is None or not isinstance(semantic_result, dict):
      logger.error("语义校验返回无效结果：description='%s'，result=%r", request.description, semantic_result)
      # 与「API异常」降级路径一致：建待审核任务（error="语义校验服务异常，已转人工审核"）、
      # 启动 _process_event（emergency_type="人工部"、status="待审核"）、返回
      # EventResponse(success=True, data={..., status="待审核"}, error=None)
  ```
- 响应契约：success=true；data.status="待审核"；data.error 为 None（响应层不回显）；data 其余字段与超时/API异常路径一致（address=""、event_type="待审核"、urgency="中"、scene_tag="常规"、handler=""）。
- 日志：内部信息（实际返回值/异常）仅 logger.error/exception 记录，绝不进入响应或任务 error。
- 复用现有「API异常」降级代码块，避免重复实现（可抽公共函数或就地复制同构代码，二选一，保持行为一致）。

### 3.2 D2 修复（main.py _process_event）
- 重构顺序：先校验、后变更状态。
  ```
  result = await asyncio.wait_for(asyncio.to_thread(_run), timeout=60.0)
  REQUIRED = ("handler", "address", "event_type", "urgency", "scene_tag")
  if not isinstance(result, dict):
      # 置处理失败（沿用状态守卫）error="事件处理结果无效"
  elif missing := [k for k in REQUIRED if k not in result]:
      # 置处理失败 error="事件处理结果缺失必需字段：" + ",".join(missing)
  else:
      # 原逻辑：状态守卫(处理中→已完成) + task.update(5 字段) + completed_at + _save_tasks
  ```
- 错误文案（精确）：
  | 场景 | error（精确匹配） |
  |---|---|
  | result 非 dict（None/列表等） | `事件处理结果无效` |
  | result 缺必需字段 | `事件处理结果缺失必需字段：<逗号分隔字段名>` |
- 状态守卫：处理失败/处理超时/已撤销等终态不可被改写（沿用「仅处理中/待审核」守卫）；缺字段校验失败不得先置「已完成」。
- 落盘：失败与成功路径均 `_save_tasks`；tasks.json 可重载出新状态。
- 安全：error 不含 Traceback、main.py、盘符路径、行号、异常类型前缀（如 "ValueError："）。

### 3.3 测试转换（test_chain_breaks.py）
- 4.3（D1）：receive_node 返回 None → 断言 success=true、data.status="待审核"、data.error is None、响应文本不含 "AttributeError"/"Traceback"/"main.py"/"C:\\"/"line "；任务 error == "语义校验服务异常，已转人工审核"。
- 2.4（D2）：invoke 返回 {"handler":"物业部"}（缺 4 字段）→ 断言 status="处理失败"、error 含 "事件处理结果缺失必需字段" 且含 "address"、不含内部标记；tasks.json 重载后为处理失败；另补 invoke 返回 None → status="处理失败"、error == "事件处理结果无效"。
- 既有 2.1/2.3/正向 2.5 等断言保持不变（证明修复未破坏原语义）。

## 四、验收命令
1. `python -m pytest tests/test_chain_breaks.py -q` 全绿（D1/D2 硬断言）。
2. 相关回归：`python -m pytest tests/test_event_cancel.py tests/test_security_authorization.py tests/test_semantic_timeout.py tests/test_input_validation.py -q` 全绿（状态机/撤销/降级不破坏）。
3. `python tests/run_regression.py core` 与 `full` 全绿、残留备份检查=无。
4. 密钥扫描 `python scripts/scan_secrets.py` 0 命中。
5. 范围检查：git diff 仅 `main.py` 与 `tests/test_chain_breaks.py`；接口契约/INTERFACE.md 无变更。

> ⚠️ 沿用约定：改前备份 data/secure；勿并行；跑前确保无 file 模式 8000 服务并发写。

## 五、产出要求
- 变更清单：main.py D1/D2 两处 diff 摘要（含错误文案）；test_chain_breaks.py 断言转换 diff 摘要。
- 测试结果：验收命令 1-5 输出摘要。
- 以【dev-ready】消息回复本任务书身份头中的 from（planner）会话。