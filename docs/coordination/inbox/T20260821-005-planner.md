【消息类型】dev-ready
【任务号】T20260821-005
【版本】1
【发送方】developer@developer-05
【接收方】planner@01a0230e-cc29-7490-bddb-9d1ae703876a
【时间】2026-08-21T16:16:30+08:00
【正文】

A) 变更清单（requirements.txt / main.py / docs/监控告警.md（新）/ deploy/DEPLOY.md /
   docs/INTERFACE.md（可选增补）/ tests/test_metrics.py（新））
- requirements.txt：+prometheus-fastapi-instrumentator==8.1.0（锁定）；+prometheus-client==0.21.0
  （锁定以保持冻结契约 Content-Type version=0.0.4，0.22+ 返回 version=1.0.0）。
- main.py：Instrumentator().add(metrics.default()).instrument(app).expose(app,
  endpoint="/metrics", include_in_schema=False)——/metrics 无鉴权、不参与业务限流
  （slowapi 仅作用于 @limiter.limit 端点）、不进 OpenAPI。
  【技术说明】任务书要点的 `Instrumentator().instrument(app).expose(...)` 在 8.1.0 下
  不注册默认 HTTP 指标（需显式 .add(metrics.default())），为满足冻结契约「含
  http_requests_total/http_request_duration_seconds」按官方 8.1.0 API 补 .add(...)；
  expose 参数与任务书一致。
- docs/监控告警.md（新建）：指标端点概览 + 指标清单表（HTTP 5 项 + 进程/Python 运行时）
  + scrape 配置示例（job=grid-assistant/端口 8000/interval 15s/compose 内 app:8000）
  + 4 条 Alertmanager 告警规则（5xx 比例/P95 耗时/实例存活/事件失败率预留）
  + 安全建议 5 条（含反向代理内网限制）。
- deploy/DEPLOY.md：新增「## 9. 监控与告警（Prometheus）」章节引用 docs/监控告警.md
  （插在第 8 章后、附录前，避免重编号既有内部交叉引用）。
- docs/INTERFACE.md：可选增补「## HTTP API（metrics）」契约（增补即冻结）。
- tests/test_metrics.py（新建 5 用例）：200+text/plain(0.0.4)+指标名+计数随访问变化+
  无鉴权+RATE_LIMIT_ENABLED=true 下 /metrics、/health 仍 200+静态断言。

B) 验收命令输出摘要
1. pytest tests/test_metrics.py -q：5 passed in 3.21s。
2. run_regression core：11 通过 / 0 失败 / 56.3s；full：24 通过 / 0 失败 / 177.5s
   （含 test_metrics 4.7s）。
3. scan_secrets.py：SECRET_SCAN_TOTAL=134 / NO SECRET HITS（0 命中）。
4. grep docs/监控告警.md 'http_requests_total|scrape|alert'：21 处命中。

C) 冻结契约逐条对照：全部满足（详见工作日志 docs/coordination/logs/developer-T20260821-005-2026-08-21.md）。

【请求】请按任务书验收；如 rejected，请附问题清单，我将逐条修复后发 fix-done。
