【消息类型】dev-ready
【任务号】T20260820-001-TC
【版本】1
【发送方】developer@developer-01
【接收方】planner@01a01a02-fd78-70e2-b6fc-97a86b7d684c
【时间】2026-08-20T03:00:00+08:00
【正文】

A) 变更清单（5 新文件 + README diff 摘要）
新增：
- .github/workflows/ci.yml：on:[push, pull_request]；job regression（checkout@v4 → setup-python@v5 3.12 →
  pip install -r requirements.txt → python -m pytest tests --collect-only -q → python tests/run_regression.py core）；
  job frontend-security（checkout → setup-python@v5 3.12 + setup-node@v4 node 20 →
  python scripts/check_frontend_js.py → python scripts/scan_secrets.py）；两 job 均不注入任何 secrets。
- .github/workflows/cloud-integration.yml：仅 on: workflow_dispatch；checkout → setup-python →
  pip install → env 注入 AUTH_STORE=cloudbase + secrets.COS_REGION/COS_BUCKET/COS_SECRET_ID/
  COS_SECRET_KEY/DATA_ENCRYPTION_KEY → python scripts/verify_cloud_integration.py。
- scripts/check_frontend_js.py：抽取 static/index.html、login.html、admin.html 内联 <script>（无 src）→
  临时 .js → node --check；任一失败退出非 0。
- scripts/scan_secrets.py：非 .env 文件扫描（排除 .git/.venv/__pycache__/.codex/.claude/node_modules/备份目录）；
  git check-ignore .env 校验；占位 pattern（COS_SECRET_(ID|KEY)\s*[:=]、
  DATA_ENCRYPTION_KEY\s*[:=]\s*[0-9a-fA-F]{32,}、AKID[0-9A-Za-z]{20,}）命中即失败；只报文件+pattern 名。
- scripts/verify_cloud_integration.py：仅操作临时对象 _ci_verify_<uuid>.enc（ensure_bucket →
  upload(encrypt) → download+decrypt 比对 → delete_object）；绝不碰 users.json.enc/sessions.json.enc、
  不打印密钥；失败退出非 0；--offline 用内存 fake 替换 cloud_store._get_client 离线单测（不触网）。
修改：
- README.md：新增「CI（GitHub Actions）」小节——ci.yml 自动触发（push/PR，离线无 secrets）、
  cloud-integration.yml 手动触发 + Secrets 配置清单（5 变量）、本地等价命令
  （run_regression core / check_frontend_js / scan_secrets / verify --offline）、推送到 GitHub 后自动生效。

B) 验收命令 1-6 输出摘要
1. python scripts/check_frontend_js.py → 3 页面 PASS / exit 0；
   python scripts/scan_secrets.py → SECRET_SCAN_TOTAL=140 / NO SECRET HITS + git check-ignore .env 命中 / exit 0。
2. YAML 结构（PyYAML 6.0.3 解析 + 人工核对）：ci.yml on=[push,pull_request] + 两 job（regression 5 步 /
   frontend-security 5 步，ubuntu-latest，无 secrets/env）；cloud-integration.yml 仅 workflow_dispatch +
   verify-cloud job env 注入 5 secrets + AUTH_STORE=cloudbase。✔
3. 密钥检查：.env 中 4 个非空真实值在非 .env 文件 grep → 0 命中（全程未打印值）。✔
4. python tests/run_regression.py core → 11 通过 / 0 失败 / 残留备份检查=无 / exit 0（总耗时 46.4s）；
   data/secure 前后快照一致；CI sanity `pytest tests --collect-only -q` → 52 collected / exit 0。✔
5. python scripts/verify_cloud_integration.py --offline → 内存 fake client（mock _get_client）不触网 PASS，
   临时对象上传/下载/解密比对/删除 OK / exit 0；无 COS 配置真实模式快速失败 exit 1（不打印密钥）。✔
6. 范围检查（T-C 基线快照 mtime+size 比对）：新增仅 .github/workflows/ci.yml、cloud-integration.yml、
   scripts/*.py（3）；修改仅 README.md + 流程文件（agent-registry 注册行、本日志、inbox 消息）；
   业务代码/测试/static/data 零改动。✔

C) 自检结论（冻结契约逐条）
3.1 ci.yml：push/PR 触发 + regression/frontend-security 两 job，步骤顺序与命令按契约，无 secrets：满足。
3.2 cloud-integration.yml：仅 workflow_dispatch，env 注入 AUTH_STORE=cloudbase + 5 secrets，默认不跑：满足。
3.3 check_frontend_js.py：3 页面内联无 src 脚本 node --check，失败退出非 0：满足。
3.4 scan_secrets.py：排除项 + git check-ignore .env + 3 占位 pattern，命中即失败、只报文件+pattern：满足。
3.5 verify_cloud_integration.py：仅临时对象 _ci_verify_<uuid>.enc 全链路，不碰真实对象、不打印密钥、
    失败非 0、支持 mock _get_client 离线单测：满足。
3.6 README CI 小节：自动/手动触发 + Secrets 配置清单 + 本地等价命令：满足。
范围：不改业务代码/测试/run_regression/conftest；不写真实密钥；不做云端数据迁移/清真实对象；不配置远程推送：
    满足（注：仓库已有 origin 远程，未执行任何 push）。
歧义/说明（请规划确认，可 rejected 附问题清单）：
- scan_secrets.py 对「文档占位符」做了误报排除：.env.example 注释示例、DEPLOY.md 的 <SecretId>、
  docker-compose.yml 的 ${COS_SECRET_ID}、cloud-integration.yml 的 ${{ secrets.X }} 引用均不命中；
  DATA_ENCRYPTION_KEY(32+hex) 与 AKID 前缀即使在注释中也照常命中。否则严格 pattern 会在既有文档误报，
  验收命令 1（0 命中）无法满足。
- verify_cloud_integration.py 使用 secure_store.encrypt/decrypt 的 kind="users"（该模块仅支持 users/sessions
  AAD 域隔离）复用正式 AES-256-GCM 路径；对象为独立临时对象，不触碰真实数据。

【请求】请按任务书验收；如 rejected，请附问题清单，我将逐条修复后发 fix-done。
