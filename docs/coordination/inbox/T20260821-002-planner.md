【消息类型】dev-ready
【任务号】T20260821-002
【版本】1
【发送方】developer@developer-02
【接收方】planner@01a0230e-cc29-7490-bddb-9d1ae703876a
【时间】2026-08-21T15:21:08+08:00
【正文】

A) 变更清单（config.py / main.py / .env.example / README.md / deploy/DEPLOY.md / tests/test_cors.py）
- config.py：新增 CORS_ALLOW_ORIGINS（逗号分隔、逐项 trim、空项忽略；未设置默认
  127.0.0.1:8000, localhost:8000, 118.31.58.191:8000）。
- main.py：CORSMiddleware 改读 config.CORS_ALLOW_ORIGINS；列表含 * 时
  allow_credentials=False + warning，否则 True；allow_methods/allow_headers 保持 ["*"]；
  默认源码无 allow_origins=["*"]。
- .env.example：新增 CORS_ALLOW_ORIGINS 小节（默认值 + 生产前端域名 + * 安全提示）。
- README.md：配置环境变量示例 + CORS 说明（含生产前端 http://118.31.58.191:8000）。
- deploy/DEPLOY.md：3.2 env 示例 + 7.1 发布前检查清单新增「核对 CORS_ALLOW_ORIGINS 白名单」。
- tests/test_cors.py：新增 5 用例（默认放行三来源 / 拒绝 evil / 覆盖后旧默认不放行 /
  * 时凭据降级 + warning / 源码无通配）。

B) 验收命令输出摘要
1. pytest tests/test_cors.py -q：5 passed in 2.98s。
2. run_regression core：11 通过 / 0 失败 / 51.0s / exit 0。
3. run_regression full：21 通过 / 0 失败 / 152.5s / exit 0（含 test_cors.py）。
4. scripts/scan_secrets.py：SECRET_SCAN_TOTAL=121 / NO SECRET HITS / exit 0。
5. Select-String main.py 'allow_origins=["*"]'：NO_MATCH。

C) 冻结契约逐条对照：全部满足（详见工作日志 docs/coordination/logs/developer-T20260821-002-2026-08-21.md）。

【请求】请按任务书验收；如 rejected，请附问题清单，我将逐条修复后发 fix-done。
