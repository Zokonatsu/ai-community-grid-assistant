【消息类型】dev-ready
【任务号】T20260821-006
【版本】1
【发送方】developer-006@developer-sub-T006
【接收方】planner@01a0230e-cc29-7490-bddb-9d1ae703876a
【时间】2026-08-21T15:06:54+08:00
【正文】

A) 变更清单（纯文档，仅 2 个文件）
1. deploy/DEPLOY.md（+159/-1）：
   - 新增 `## 7. 标准化回滚 SOP`：7.1 发布前检查清单（备份 data/、secure/、.env 与 DATA_ENCRYPTION_KEY、记录当前镜像 tag）；7.2 回滚步骤（docker compose down → 恢复备份 → 上一版本镜像 up → curl /health 验证 → 抽查数据可解密）；7.3 回滚决策与验证标准；7.4 注意事项（events.jsonl 追加不回滚、tasks.json 以备份为准、密钥与加密文件必须成对恢复）。
   - 新增 `## 8. 扩容与多实例`：8.1 单实例约束说明（内存 _tasks、本地写锁、secure/ 本地加密文件、events.jsonl 追加并发）；8.2 方案 A 单机升配 + AUTH_STORE=cloudbase 账号上云；8.3 方案 B 多副本需共享 data 卷或对象存储 + events.jsonl 多进程追加并发风险与建议；8.4 Nginx 多 upstream 示例。
   - 第 1 章高可用提示行加交叉引用指向第 8 章。UTF-8 无 BOM、CRLF 行尾。
2. README.md（+4）：数据持久化、账号数据加密存储两处交叉引用上述两章节。

B) 验收命令输出摘要
1. Select-String deploy\DEPLOY.md '回滚|扩容|多实例'：命中，含检查清单（L336）/ 回滚步骤（L349）/ 验证命令（L382）。
2. Select-String README.md '回滚|扩容'：2 处命中（L354、L372）。
3. git diff --stat（本任务）：仅 README.md 与 deploy/DEPLOY.md；其余文件为并行子代理改动（deploy/Dockerfile、docs/INTERFACE.md、agent-registry、tests/test_auth.py）。
4. .venv\Scripts\python.exe tests\run_regression.py core：11/11 PASS，0 失败，总耗时 44.2s，退出码 0。

【请求】请按任务书验收；如 rejected，请附问题清单，我将逐条修复后发 fix-done。
