【消息类型】dev-ready
【任务号】T20260819-004
【版本】1
【发送方】developer-01@developer-01
【接收方】planner@01a01a02-fd78-70e2-b6fc-97a86b7d684c
【时间】2026-08-19T23:20:00+08:00
【正文】

A) 变更清单（文件 + diff 摘要）
1. cloud_store.py（+55 行）
   - 新增 SESSIONS_OBJECT_KEY = "sessions.json.enc"。
   - 新增 ensure_bucket()：head_bucket 存在即跳过；404 则 create_bucket(ACL="private")；异常 fail-fast raise；不打印密钥，仅记桶名。
   - 新增 object_exists(key) / delete_object(key)：供 init_cloud_storage.py 清空旧对象；对象不存在语义正确，异常 raise。
2. auth.py（会话分发改造）
   - 新增 _load_sessions()/_save_sessions()：file → 本地加密文件；cloudbase → cloud_store.download/upload(SESSIONS_OBJECT_KEY) + secure_store encrypt/decrypt（None→空库，异常 fail-fast）。
   - 替换全部会话读写点：_init_auth 加载、login_user、logout_user、get_current_user 过期清理、_cleanup_expired_sessions → 全部走 _load_sessions/_save_sessions；grep 确认无残留直接写本地 sessions 的调用（仅 file 模式迁移函数内保留 _save_json，且 cloudbase 下 early return）。
   - _init_auth：cloudbase 下先 ensure_bucket() 再 _load_users/_load_sessions；空库重建 admin 后 _save_users + _save_sessions 一并上云。
   - 头部注释同步（cloudbase 下用户+会话均上云，本地 secure 非权威）。
3. .env：AUTH_STORE 由 file 改为 cloudbase（仅此一个值；COS_*、DATA_ENCRYPTION_KEY、LLM_* 均未动、未打印、.env 不在 git 跟踪）。
4. init_cloud_storage.py（新增，幂等）：建桶（私有 ACL）→ 清空云端 users.json.enc/sessions.json.enc（存在才删）→ 删除本地 secure/users.json.enc、secure/sessions.json.enc、data/users.json、data/sessions.json、项目内 *.migrated.bak → 摘要输出（桶名/对象 key/删除数，不含密钥）；无参数交互二次确认，--yes/--force 跳过；仅删身份/会话数据，data/events.jsonl、tasks.json、community_config.json 绝不触碰。
5. tests/test_cloud_store.py（6→13 用例，全离线 fake client）：新增 ensure_bucket 缺失创建（私有 ACL）/已存在跳过/异常 raise、object_exists/delete_object 语义、空库重建 admin 上云+不写本地 secure、会话云端写/读/删闭环、新注册居民身份上云（加密，无明文落云）。
6. tests/test_*.py（16 个）：全部在 import config/auth/main 前固定 os.environ["AUTH_STORE"]="file"（test_auth/cloud_store/community_center/comprehensive/data_isolation/event_cancel/geo/input_validation/life_rescue_fix/proxy_beneficiary/register_location/resident_review_location/scene_tag/security_fixes/semantic_timeout/server）。
7. README.md / DEPLOY.md：各加一句本地模式用法（临时 $env:AUTH_STORE='file' 或独立 .env），DEPLOY 会话行改为「云端对象 sessions.json.enc，加密上传、登出即删」；均不含密钥。

B) 测试结果
1. python tests/test_cloud_store.py（.venv 绝对路径，PYTHONIOENCODING=utf-8）：13/13 PASS（离线 fake client，未访问真实网络）。
2. 全量回归（逐个串行，跑前独立备份 data/secure 至 _dev01_backup_20260819_T004）：14/15 PASS——test_auth、test_cloud_store、test_community_center、test_comprehensive、test_data_isolation、test_event_cancel、test_input_validation、test_life_rescue_fix、test_proxy_beneficiary、test_register_location、test_resident_review_location、test_scene_tag、test_security_fixes、test_semantic_timeout 全绿；每跑后 data/secure 哈希与跑前一致、无残留 *.bak.*。
   - test_geo.py 批跑 8/10：既有环境性失败（同 T003 轮）——真实 data/community_config.json 中心（28.368178/121.356875）≠ 测试硬编码默认中心（30.274150/120.155150）；geo.py/community_store.py/data 均非本次改动；隔离（临时移走 community_config.json）重跑 10/10 通过，已恢复且哈希一致。
   - test_server.py 未跑：远程部署主机 118.31.58.191:8000 冒烟，非本地回归（同前惯例）。
3. 静态检查：.env AUTH_STORE=cloudbase；全仓 grep（排除 .venv/备份/git）确认 COS_SECRET_ID/COS_SECRET_KEY/DATA_ENCRYPTION_KEY/LLM_API_KEY 明文仅存在于 .env（0 命中非 .env 文件）；.env 未被 git 跟踪。

C) 自检结论（冻结契约逐条）
1. 建桶：ensure_bucket() head/create、私有 ACL、存在跳过；全程不打印 COS_SECRET_ID/KEY（用例覆盖缺失创建/存在跳过/异常 raise）。✓
2. 加密：cloudbase 云端 users.json.enc/sessions.json.enc 均为 secure_store.encrypt AES-256-GCM blob；离线+联网均验证解密还原 JSON、无明文落云（blob 不含 username/password_hash/token 明文）。✓
3. sessions 上云：登录写、登出删、get_current_user 校验、过期清理、_cleanup_expired_sessions 均走云端 sessions.json.enc；本地 secure/sessions.json.enc 不再作为权威（联网验证本地 secure/ 无新增 *.enc）。✓
4. 旧数据清理：init_cloud_storage.py 已执行——云端两对象清空、本地 secure/users.json.enc、secure/sessions.json.enc 删除；data/users.json、sessions.json、*.migrated.bak 均不存在；data/events.jsonl、tasks.json、community_config.json 与备份逐字节一致。✓
5. 空库重建：云端空/不存在 → 服务启动自动重建 admin/admin123456 并上传 users+sessions（联网 8011 实例验证：users 解密含 admin、sessions 空表）；新注册居民上云（users 解密含 admin+新居民 count=2）。✓
6. 测试隔离：16 个 tests/test_*.py 全部 import config/auth/main 前固定 AUTH_STORE=file；test_cloud_store.py 离线适配并新增 ensure_bucket/sessions/加密闭环/空库重建 admin 上云用例，不访问真实网络。✓
7. 安全：密钥仅存 .env、未打印、未入文档/日志/知识库（grep 0 命中）；日志仅含桶名/对象 key/字节数。✓
8. 明确不做：未改前端、未改 migrate_to_cloud.py、未迁移旧数据、未清业务数据、未新增改密码功能。✓

D) 联网验证结果（验收命令 4，已执行）
- python init_cloud_storage.py --yes：桶 zhangmi-1445045235（ap-guangzhou）已存在（跳过创建，私有 ACL）→ 云端 users.json.enc 删除、sessions.json.enc 不存在跳过 → 本地 secure 两文件删除 → 摘要不含密钥；二次运行幂等（对象清空、本地 0 删除）。
- 隔离实例验证（另起 127.0.0.1:8011，cloudbase，不干扰主 Agent 8000 服务）：启动后空库重建 admin 上云 → HTTP 注册新居民成功 → 云端 users.json.enc 解密含 admin+新居民、sessions.json.enc 解密含登录 token → 本地 secure/ 无任何新增 *.enc；验证后已停止 8011 实例，8000 主服务（PID 21572）未受影响。

【协调提示】主 Agent 的 8000 服务仍以 file 模式在内存持有旧账号（admin/inside_201225/kiki），需由主 Agent 重启该服务才会切到 cloudbase 并从空云库重建 admin（旧账号随本地 secure 删除而消失，符合「删除、不迁移」）。

【请求】请按任务书验收；如 rejected，请附问题清单，我将逐条修复后发 fix-done。
