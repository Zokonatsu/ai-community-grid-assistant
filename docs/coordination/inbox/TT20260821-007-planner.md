【消息类型】dev-ready
【任务号】T20260821-007
【版本】1
【发送方】developer@developer-01
【接收方】planner@01a0230e-cc29-7490-bddb-9d1ae703876a
【时间】2026-08-21T15:10:43+08:00
【正文】

A) 变更清单
1. tests/test_auth.py：
   · 新增 logout(token=None) 工具函数（POST /api/auth/logout）。
   · 7.3 重写为硬断言：重新登录 → logout 200 且 body=={"message":"登出成功"} → 同一 token /me 401
     且 detail=="未登录或登录已过期，请重新登录"。
   · 7.4 重写为硬断言：登出前 /me 200 → 登出 200 → 登出后同一 token /me 401（token 立即失效）；
     7.4b 新增无 token / 无效 token 调 logout 均 200 + {"message":"登出成功"}（幂等）。
   · 删除「服务端缺少 /api/auth/logout」「旧Token仍有效（服务端未清除）」错误文案。
2. docs/INTERFACE.md：新增「## HTTP API（logout）」契约（POST /api/auth/logout，可带 Bearer token 无请求体；
   带有效 token 200+{"message":"登出成功"}并删服务端 session；无/无效 token 幂等 200 不抛错；
   登出后同一 token /me 401「未登录或登录已过期，请重新登录」）。

B) 验收命令 1-5 输出摘要
1. .venv\Scripts\python.exe -m pytest tests/test_auth.py -q → 1 passed / exit 0。
2. Select-String tests\test_auth.py '服务端缺少|旧Token仍有效' → 无命中；INTERFACE.md 含 logout 契约。
3. run_regression core → 11 通过 / 0 失败 / 残留备份=无 / exit 0。
4. run_regression full → 20 通过 / 0 失败 / 残留备份=无 / exit 0。
5. scan_secrets → SECRET_SCAN_TOTAL=115 / NO SECRET HITS / exit 0。

未改 main.py/auth.py/前端；未 git add/commit/push。详见 docs/coordination/logs/developer-TT20260821-007-2026-08-21.md。