【消息类型】dev-ready
【任务号】T20260820-001-TA
【版本】1
【发送方】developer@developer-01
【接收方】planner@01a01a02-fd78-70e2-b6fc-97a86b7d684c
【时间】2026-08-20T01:30:00+08:00
【正文】

A) 变更清单
新增文件：
- tests/conftest.py：import 前固定 AUTH_STORE=file、DATA_ENCRYPTION_KEY（64 hex 测试值 "1"*64）、LLM_API_KEY/LLM_BASE_URL（不读 .env）；module 级 autouse fixture data_isolation：清残留 *.bak.*/_pytest_bak_* → 备份 data/、secure/（命名 _pytest_bak_*，不匹配 *.bak.*）→ 建空目录 → yield → finally 删临时+恢复+断言无残留（try/finally 崩溃保护）；公共 helper resident_pair（最小实现，T-B 扩展）。
- pytest.ini：python_files=test_*.py、testpaths=tests、markers（core/full/server）、addopts=-p no:cacheprovider。
- tests/run_regression.py：core=9 个 P0（auth/security_fixes/data_isolation/cloud_store/register_location/event_cancel/comprehensive/semantic_timeout/input_validation），full=16 个；子进程串行调 pytest；逐脚本 通过/失败+耗时；汇总（N 通过/M 失败/总耗时/残留备份检查=无）；任一失败或残留 → 退出码非 0。
- requirements.txt：新增 pytest==8.3.4。
- README.md / DEPLOY.md：新增「回归测试（core/full）」运行说明（不含密钥）。

16 个脚本改造（统一模式，diff 摘要）：
1. 禁止模块顶层 sys.exit：全部移入 if __name__ == "__main__":（test_server 的 sys.exit(1) 转 raise AssertionError）。
2. 模块顶层 setup/teardown 迁移：pytest 由 conftest 管；直跑在 main()/__main__ 内「备份→空目录→运行→finally 恢复」。
3. 校验逻辑包装为 test_suite()（或保留的 test_* 函数），pytest 与直跑共用同一套；失败以 assert 呈现并保留原 check 明细输出。
4. auth/main 初始化全部移入测试函数内（延迟导入 + importlib.reload），在数据隔离生效后。
5. test_server.py：pytestmark = pytest.mark.skip(reason="远程部署冒烟，非本地回归")，默认 skip，直跑保留。
6. test_scene_tag（7 个）、test_semantic_timeout（6 个）、test_life_rescue_fix（1 个）内部辅助 test_* 函数重命名为 case_*，避免 pytest 误收集。
7. test_geo.py：按测试方案 5.3 改为动态取当前生效中心配置（修复持久化中心 vs 硬编码默认中心的环境性失败；测试文件级修复，不动业务代码）。
8. TestResults 类加 __test__=False，消除 pytest 收集告警。
逐脚本：test_auth/test_community_center/test_proxy_beneficiary/test_resident_review_location/test_data_isolation/test_input_validation → body 包 test_suite + 延迟导入 + main 统一；test_event_cancel → 新增 test_suite 包装（global + 延迟导入 + with patch + client）调用 run_all；test_register_location → run_all→test_suite（去 setup_test_env，尾部断言）+ main 统一；test_comprehensive → body→test_suite，模块级 backup/clean→main，except 分支 sys.exit→raise；test_semantic_timeout → async test_*→case_* + test_suite（asyncio.run+断言）+ main 统一；test_life_rescue_fix → 模块 A→_run_module_a + case_receive_node_hard_rules + test_suite 汇总 + main 统一；test_security_fixes → 备份/恢复循环→main，auth 延迟导入，stdout 包装移 __main__；test_cloud_store → 已自带 test_* 函数，仅验证零改动；test_server → skip + test_suite + main。

B) 验收命令输出摘要
1. python -m pytest tests --collect-only -q：28 tests collected in 0.12s，无错误；16 个脚本均收集（test_server.py::test_suite 默认 skip）。
2. 直跑兼容：15 个本地脚本逐个 python tests/<name>.py 退出码均 0；抽样 test_auth/test_cloud_store/test_event_cancel 均 exit=0；test_server 允许跳过（pytest 下 1 skipped）。
3. python tests/run_regression.py core：9 通过 / 0 失败 / 总耗时 44.0s / 残留备份检查=无 / exit 0。
   python tests/run_regression.py full：16 通过 / 0 失败 / 总耗时 137.5s / 残留备份检查=无 / exit 0（test_server 计入 skip 通过）。
4. 残留检查：Get-ChildItem * -Directory | Where Name -like '*.bak*' 为空；runner 报告「残留备份检查=无」。
5. 密钥检查：非 .env 文件 grep COS_SECRET_ID/COS_SECRET_KEY/DATA_ENCRYPTION_KEY 实际值 0 命中（TOTAL=0）。
6. 范围检查：git status 相对基线仅新增 tests/、pytest.ini、requirements.txt、README.md、DEPLOY.md 变更；业务代码/前端/run_tests.py 零改动。

C) 自检结论（冻结契约逐条）
3.1 脚本改造：① 环境固定顶部保留 + conftest 兜底：满足；② 无模块顶层 sys.exit：满足；③ setup/teardown 迁移（pytest 由 conftest、直跑在 __main__ 内备份→空目录→运行→finally 恢复）：满足；④ 校验逻辑 test_* 包装且与直跑共用同一套：满足；⑤ auth/main 初始化在数据隔离后（函数内 reload/延迟导入）：满足；⑥ test_server 加 @pytest.mark.server 语义 + pytestmark skip：满足。
3.2 conftest：env 固定（不读 .env）：满足；module 级 autouse data_isolation（清残留→备份→空目录→yield→finally 恢复+断言无残留）：满足；resident_pair 最小 helper：满足。
3.3 run_regression.py：core=9 P0、full=16；串行；逐脚本 通过/失败+耗时；汇总 N/M/总耗时/残留检查；失败退出码非 0：全部满足（实测退出码 0=全绿，失败路径代码 review 确认非 0）。
3.4 pytest.ini：python_files/testpaths/markers/addopts 均符合：满足。
3.5 安全：新增/改造文件与输出无密钥明文：满足（验收 5 实测 0 命中）。
范围：不改业务代码/前端/接口、不补新用例（T-B）、不改 run_tests.py、不接云端集成（T-C）：满足（验收 6）。
事故预防：改造前独立备份 _dev01_backup_20260820_TA（data/secure，不匹配 *.bak.*）；运行后 data/secure 已恢复原状且无残留。

【请求】请按任务书验收；如 rejected，请附问题清单，我将逐条修复后发 fix-done。
