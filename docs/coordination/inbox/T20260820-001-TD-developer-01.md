【消息类型】dev-ready
【任务号】T20260820-001-TD
【版本】1
【发送方】developer@developer-01
【接收方】planner@01a01a02-fd78-70e2-b6fc-97a86b7d684c
【时间】2026-08-20T03:05:00+08:00
【正文】

A) 变更清单（强度测试/探针 + 各修改 diff 摘要，含警告修复原因）
新增：
- tests/_infra_probe_fail.py（探针，不以 test_ 开头、不被常规收集）：一个用例故意 assert False，
  仅供强度测试以子进程方式显式运行（python -m pytest tests/_infra_probe_fail.py -q）。
- tests/test_infra_strength.py（入 FULL 不入 CORE）：子进程跑探针，断言退出码非 0 且输出含
  assert False；断言运行前后项目根目录备份残留集合不变（无新增/无误删）、data/secure 快照
  （文件清单 + SHA256）与运行前一致；data/secure 预置标记文件增强「原样」比较。

修改：
- tests/conftest.py（最小扩展，嵌套/崩溃场景安全版）：
  · 备份命名 _pytest_bak_data_<pid>_<uuid>（含属主 PID）；
  · _cleanup_residue 仅删除「属主进程已退出」的陈旧 _pytest_bak_*，跳过活备份
    （Windows=ctypes.OpenProcess，POSIX=os.kill(pid,0)）；teardown 残留断言改为「无陈旧残留」。
  · 原因：修复 T-B P3 观察（多进程/直跑组合后 data/secure 遗留测试态）——嵌套 pytest
    子进程的 cleanup 会误删外层活备份；若不修，强度测试的探针子进程会删掉外层真实
    data/secure 备份导致静默丢失。
- tests/test_register_location.py：test_suite 末尾删除 return 0/1（保留 assert failed==0 语义），
  直跑 main() 仍按结果 sys.exit(0/1) → 消除 PytestReturnNotNoneWarning。
- tests/test_event_cancel.py：simulate_timeout 的 wait_for mock 由 side_effect=TimeoutError()
  改为 async side_effect（创建任务→cancel→await 消费 to_thread 协程→抛 TimeoutError），与真实
  wait_for 超时语义一致 → 消除「coroutine 'to_thread' was never awaited」RuntimeWarning。
  注：patch.object 对 async 函数自动生成 AsyncMock，side_effect 必须是 async 函数才会被消费。
  未用全局 warnings 抑制。
- pytest.ini：filterwarnings 精确抑制
  ignore:Using `httpx` with `starlette\.testclient` is deprecated:starlette.exceptions.StarletteDeprecationWarning
  （注释：starlette 1.6.0 + fastapi 0.139.0 + httpx 0.28.1 依赖行为，暂不升级依赖）。
- tests/run_tests.py（仅文件头）：docstring 注明「实时冒烟脚本：需服务已启动（默认
  127.0.0.1:8000）且 TOKEN 有效；非 pytest 回归套件；回归请用 tests/run_regression.py core/full」，
  不改任何行为。
- README.md：新增「run_tests.py 与 test_server.py 定位」小节（run_tests=实时冒烟；
  test_server=远程部署主机 118.31.58.191 冒烟、本地默认 skip、不入 CI）；计数 19→20。
- tests/run_regression.py：FULL 由 os.listdir 动态计算自动纳入 test_infra_strength（CORE 不含）；
  CORE 11 个与 ci.yml（core）及 README 一致。

B) 验收命令 1-6 输出摘要
1. python -m pytest tests/test_infra_strength.py -q → 1 passed / exit 0。
2. python -m pytest tests/test_register_location.py tests/test_event_cancel.py -q
   -W error::pytest.PytestReturnNotNoneWarning -W error::RuntimeWarning → 2 passed / exit 0，
   无 PytestReturnNotNoneWarning / coroutine RuntimeWarning；cov 全量输出
   （run_regression.py cov 与 README#4 直接命令）均无这两类警告，亦无 StarletteDeprecationWarning。
3. python -m pytest tests --collect-only -q → 53 collected / exit 0，无收集错误；探针未被收集。
4. python tests/run_regression.py core → 11 通过 / 0 失败 / 残留备份检查=无 / exit 0（46.8s）；
   python tests/run_regression.py full → 20 通过 / 0 失败 / 残留备份检查=无 / exit 0（142.2s，
   含 test_infra_strength 3.5s）。
5. README 含 run_tests.py / test_server.py 定位说明；run_tests.py 文件头有定位注释。✔
6. python scripts/scan_secrets.py → SECRET_SCAN_TOTAL=144 / NO SECRET HITS + git check-ignore .env
   命中 / exit 0；范围：本任务仅改 tests/（conftest/探针/强度/两警告文件/runner）、pytest.ini、
   run_tests.py、README + 流程文件（agent-registry、本日志、inbox 消息）；业务代码零改动
   （业务 .py mtime 均 2026-08-19，早于本任务）。

C) 自检结论（冻结契约逐条）
1. 崩溃保护强度测试：探针子进程退出码非 0；结束后无新增备份残留、data/secure 快照
   （清单+SHA256）与运行前一致；入 FULL 不入 CORE：满足。
2. 警告清理：register_location 去返回值（保留断言、直跑仍 sys.exit）；event_cancel 定位到
   wait_for 超时路径正确取消/消费 to_thread 协程（无全局吞警告）；pytest.ini filterwarnings
   精确抑制 StarletteDeprecationWarning 并注释依赖行为、不升级依赖：满足。
3. 定位说明：run_tests.py 文件头注明实时冒烟/需服务+TOKEN/非回归套件；README 新增
   run_tests.py 与 test_server.py 定位小节（118.31.58.191、本地默认 skip、不入 CI）：满足。
4. 一致性：CORE/FULL 与 ci.yml（core 11 个）及 README 一致；强度用例入 FULL 不入 CORE：满足。
5. 安全/范围：不写真实密钥；不改业务代码/测试断言语义；不升级依赖；不改 run_tests.py 与
   test_server.py 执行行为：满足。
6. 说明项（供规划参考，不阻塞）：
   - T-A P3-整理（_planner_backup_TA_*、_dev01_backup_*、agent.md 等未申报文件收口前清理或
     说明归属）：本次未删除任何备份目录（不可逆操作需主 Agent 确认）；归属已在日志登记
     （_dev01_backup_20260820_TD 为本任务独立备份）。建议收口阶段统一处理。
   - P2-运维（两个 uvicorn 8000 进程）：本次运行前检查 8000 端口无监听、无 uvicorn 进程，
     数据隔离运行安全；后续收口仍建议确认无 file 模式服务并发写。

【请求】请按任务书验收；如 rejected，请附问题清单，我将逐条修复后发 fix-done。
