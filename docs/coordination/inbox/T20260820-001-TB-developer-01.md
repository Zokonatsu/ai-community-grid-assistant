【消息类型】dev-ready
【任务号】T20260820-001-TB
【版本】1
【发送方】developer@developer-01
【接收方】planner@01a01a02-fd78-70e2-b6fc-97a86b7d684c
【时间】2026-08-20T02:10:00+08:00
【正文】

A) 变更清单（含与既有用例去重说明）
新增：
- tests/test_security_authorization.py（CORE，8 用例）：越权矩阵 + 八项攻防。
  ① BOLA 水平越权：A 用自己 token GET/cancel/mark_read B 的事件 → 403（精确文案「无权访问该事件/无权操作该事件/无权访问此事件」）且 B 事件状态与已读时间零副作用；不存在事件 → 404。
  ② BFLA 垂直越权：居民 → GET/PUT /api/admin/users|community、accept、reply → 403「权限不足，仅管理员可访问」；无 token → 401。
  ③ API2 认证失效：无/伪造/过期/登出复用/缺 Bearer → 401（精确文案）。
  ④ BOPLA/列表隔离：居民列表仅自己事件；他人身份证/手机零泄露；event_lat/lng/event_distance_m 仅 admin 返回；提交携带 role=admin/他人 user_id/伪造 reviewer 被忽略。
  ⑤ API4 资源限制：description>500 → 422、username>20/<3 → 422；超长回复不 500（现状无长度上限+无限流，注释记录基线）。
  ⑥ API8 安全配置：404/422/401 响应零堆栈/路径/密钥；默认凭据 admin/admin123456 风险基线断言（待改密）；CORS allow_origins=['*'] 基线记录（待收紧）。
  ⑦ API10 注入与不安全消费：HTML/脚本描述与回复后端原样存储不执行；LLM 坏 JSON → 待审核降级、零内部细节透传。
  ⑧ API9 资产盘点：app.routes（13 个 /api 路由）vs 文档化接口清单比对，未文档化/未注册均为空（不阻塞输出清单）。
- tests/test_chain_breaks.py（CORE，6 用例）：四类断链。
  ① 检索未命中：问候/闲聊/宠物死亡 → success=false「输入内容无效…」、零工单、events.jsonl 零新增。
  ② 工具调用失败：dispatch_record_workflow.invoke 抛 RuntimeError/KeyError(字段缺失) → _process_event 置「处理失败」并落盘 tasks.json；error 无堆栈/路径。
  ③ 超时降级：60s 后台处理超时 → 「处理超时」+落盘（50s 语义超时→待审核 已由 test_semantic_timeout.case_timeout 覆盖，去重不重复）。
  ④ 错误结果直接展示：LLM 坏 JSON → 待审核、响应 error 为空、任务 error 仅类型名无堆栈/路径；空 dict/None 结果不 500（现状记录 D1）。
- tests/test_mutation_effectiveness.py（FULL，4 用例）：4 类守卫造错有效性，每 mutation 独立 test 函数隔离，基线绿→变异红（pytest.raises(AssertionError)）。
  M1 role 校验（移除 auth.register_user 的 admin 拒绝分支）；M2 归属校验（移除 cancel 归属判断，已解析用户直调端点——FastAPI 装饰期捕获 endpoint，patch 后 HTTP 路由不生效，故直调）；M3 超时降级（移除 _process_event 超时分支）；M4 输出转义（替换 escapeHtml 为不转义实现）。
- requirements.txt：+pytest-cov==6.0.0。

修改：
- tests/run_regression.py：CORE +security_authorization +chain_breaks（11 个）；FULL 19 个（mutation 仅 full）；新增可选 cov 子命令。
- tests/conftest.py：仅追加 admin_token / event_seed 两个公共 helper；env 固定与 data_isolation 行为零改动。
- tests/test_cloud_store.py：追加 6 个覆盖率用例（真实 _get_client 路径/缓存、SDK 缺失 ImportError、真实 _is_not_found 404/非404/防御分支、下载读响应异常、建桶失败、对象检查异常）→ cloud_store 73%→100%。
- README.md：回归说明更新（19 脚本/11 core）+ 覆盖率命令 + 单进程兼容说明。

与既有用例去重：test_data_isolation（列表隔离/详情403）→补 mark_read/cancel 无副作用、admin-only 字段裁剪、BOPLA 字段忽略；test_event_cancel（非本人 cancel 403）→补无副作用矩阵；test_security_fixes（auth 层 role=admin 拒绝）→补 API 层精确文案+非法 role 422；test_auth（401 主路径）→补过期/登出复用/缺 Bearer；test_semantic_timeout（50s 超时）→只补 60s 处理超时与 dispatch/record 抛错/坏返回；test_input_validation（机械层）→补链路级零工单零落盘；test_cloud_store 原 13 用例保留仅追加未覆盖分支。

B) 验收命令 1-6 输出摘要
1. pytest 三脚本 -q：18 passed（8+6+4），全绿。
2. 三脚本直跑：python tests/test_security_authorization.py / test_chain_breaks.py / test_mutation_effectiveness.py 均 exit=0。
3. run_regression.py full：19 通过 / 0 失败 / 残留备份检查=无 / exit 0（总耗时 142.1s）。
4. 覆盖率（--ignore=tests/test_scene_tag.py 运行，50 passed）：
   Name            Stmts   Miss  Cover
   auth.py           264     47    82%
   cloud_store.py     93      0   100%
   main.py           485     77    84%
   TOTAL             842    124    85%
   → 核心模块全部 ≥80%（cloud_store 由 73% 补齐至 100%）。
   ⚠️ 精确命令 `pytest --cov=... tests -q` 在单进程下因既有兼容问题失败：test_scene_tag 的 main.receive_node 在 import 期绑定，同进程先导入 main 的模块会污染其 API 用例（与 T-B 无关，可复现 `pytest tests/test_auth.py tests/test_scene_tag.py`，T-A 的 runner 用子进程正是绕开此问题）；覆盖率数值已用 --ignore 等价口径取得，`python tests/run_regression.py cov` 子命令同样可用。
5. mutation 报告：MUTATION REPORT: 4 捕获 / 4 总数（未捕获 0）——M1 role 校验 / M2 归属校验 / M3 超时降级 / M4 输出转义 全部「基线绿→变异红」被捕获。
6. 密钥扫描：4522 个非 .env 文件 grep COS_SECRET_ID/COS_SECRET_KEY/DATA_ENCRYPTION_KEY/LLM_API_KEY 真实值 → SECRET_SCAN_TOTAL=0 / NO SECRET HITS。
7. 残留检查：*.bak.* / _pytest_bak_* 无残留；data/secure 已从独立备份恢复并逐字节校验一致。

发现的现状风险（未改业务代码，供规划裁决/后续任务）：
- D1（安全，建议修复）：receive_node 返回 None 时，create_event 最外层兜底把内部异常回显给居民：error='事件提交失败：AttributeError：'NoneType' object has no attribute 'get''，违反「error 不含内部异常」。建议外层兜底改通用文案并记日志。
- D2（防御性缺口）：_process_event 成功分支先置 status=已完成再 task.update，若 invoke 返回缺字段 dict 会「表面成功」（已完成+空字段），非「处理失败」；LangGraph 状态合并下实际难触发。
- 观测：多进程/直跑组合后 data/secure 曾遗留测试态（疑 conftest _cleanup_residue 对未恢复备份的清理时序），已从 `_dev01_backup_20260820_TB` 恢复；建议规划核查加固。

C) 自检结论（冻结契约逐条）
3.1 越权矩阵：水平/垂直/BOPLA/列表隔离/role 伪造注册拒绝（精确文案「禁止通过注册创建管理员账号」、非法 role 422）全部覆盖并全绿：满足。
3.2 八项攻防：BOLA/BFLA/认证失效/过度数据暴露/资源限制/安全配置/注入与不安全消费/资产盘点 逐项 ≥1 且精确状态码+文案：满足。
3.3 四类断链：检索未命中/工具调用失败/超时降级(50s 去重+60s 补齐)/错误结果直接展示 各 ≥1 全绿：满足。
3.4 造错有效性：4 类守卫（role/归属/超时降级/转义）变异注入，原用例全部变红，报告 4/4 捕获、未捕获 0：满足。
3.5 覆盖率：auth 82% / cloud_store 100% / main 84%（全部 ≥80%）；精确命令的单进程 scene_tag 兼容问题已如实说明并以等价口径出报告：满足（附缺口清单见上）。
3.6 基建兼容与安全：三脚本可 pytest 收集+直跑 exit=0；云存储全离线 fake client；无 *.bak.* 残留；密钥 0 命中；业务代码零改动（mtime 校验）；test_server 远程项未跑：满足。
范围：不改业务代码/接口/契约；不真实压测/网络攻击/LLM 调用；与既有用例去重：满足。

【请求】请按任务书验收；如 rejected，请附问题清单，我将逐条修复后发 fix-done。
