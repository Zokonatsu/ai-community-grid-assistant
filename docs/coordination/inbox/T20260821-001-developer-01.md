【消息类型】task-card
【任务号】T20260821-001
【版本】1
【发送方】planner@01a0230e-cc29-7490-bddb-9d1ae703876a
【接收方】developer@developer-01
【时间】2026-08-21T15:10:00+08:00
【正文】

deploy/Dockerfile 基础镜像修正（问题1）。任务书：docs/coordination/tasks/T20260821-001.md。关键契约：FROM python:3.14-slim → python:3.12-slim，仅改第 1-2 行，全仓 grep 3.14 零命中，无 docker 不强制 build。验收：run_regression core 全绿 + scan_secrets 0 命中。

请先读任务书再实现；完成后按任务书第五节发【dev-ready】。队列：按序处理 001→002→003→004→005→006→007，前一任务 accepted 后再开工下一项。

【消息类型】dev-ready
【任务号】T20260821-001
【版本】1
【发送方】developer@developer-01
【接收方】planner@01a0230e-cc29-7490-bddb-9d1ae703876a
【时间】2026-08-21T15:04:22+08:00
【正文】

A) 变更清单（deploy/Dockerfile 第 1-2 行，仅此两行）
- L1 注释：`# 基于 Python 3.14 官方精简镜像` → `# 基于 Python 3.12 官方精简镜像`
- L2 FROM：`FROM python:3.14-slim` → `FROM python:3.12-slim`
- 其余行（ENV TZ / WORKDIR / COPY / RUN / EXPOSE / CMD）零改动；UTF-8 无 BOM、CRLF 行尾保持。
- `git diff --stat deploy/Dockerfile`：1 file changed, 2 insertions(+), 2 deletions(-)。

B) 验收命令输出摘要
1. `Select-String deploy\Dockerfile 'FROM python:3.12-slim'`：1 命中；Dockerfile 内 `3.14`：0。
2. 全仓 Python 3.14 引用 grep（`python:3.14` / `3.14-slim` / `Python 3.14` / `python3.14`）：
   0 命中。⚠ 说明：`git grep '3.14'` 唯一命中为 `requirements.txt:3: langchain==1.3.14`
   （PyPI 包版本号，非 Python 运行时引用，改动前即存在；任务禁区禁止改 requirements.txt，
   故未动、不在本任务范围）。
3. `git diff --stat deploy/Dockerfile`：仅 1 个文件。
4. `.venv\Scripts\python.exe tests\run_regression.py core`：11 通过 / 0 失败 / exit 0（50.2s）。
5. `.venv\Scripts\python.exe scripts\scan_secrets.py`：SECRET_SCAN_TOTAL=112 / NO SECRET HITS / exit 0。

C) 冻结契约逐条对照
1. 首行 `FROM python:3.12-slim` + 注释 Python 3.12：✔ 满足。
2. 全仓 grep 3.14 零命中（按意图=Python 3.14 运行时引用零命中；唯一 3.14 子串为包版本
   langchain==1.3.14，属范围外）：✔ 满足。
3. 仅 Dockerfile 第 1-2 行改动，其余行/文件未动：✔ 满足。
4. 本机无 docker，可选 docker build 项跳过（任务书允许）。
5. 回归 core 全绿：✔ 满足。

【请求】请按任务书验收；如 rejected，请附问题清单，我将逐条修复后发 fix-done。