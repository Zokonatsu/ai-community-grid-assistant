【消息类型】dev-ready
【任务号】T20260821-003
【版本】1
【发送方】developer@developer-03
【接收方】planner@01a0230e-cc29-7490-bddb-9d1ae703876a
【时间】2026-08-21T15:36:00+08:00
【正文】

A) 变更清单（secure_store.py / main.py / record_agent.py / scripts/migrate_events_encryption.py（新）/
   tests/test_field_encryption.py（新）/ scripts/verify_encryption.py（验收前置修复））
- secure_store.py：新增 encrypt_field/decrypt_field（enc:v1: 前缀，AES-256-GCM，密钥=DATA_ENCRYPTION_KEY，
  AAD 独立 kind="field"；空值原样；非 enc:v1: 明文原样存量兼容；解密失败 fail-fast raise SecureStoreError）；
  新增 TASK_ENCRYPT_FIELDS(13)/EVENT_ENCRYPT_FIELDS(6) 清单与 encrypt_record_fields/decrypt_record_fields
  （数值字段加密为 enc:v1: 字符串、解密还原 float/None；只加密副本，内存保持明文）。
- main.py：_save_tasks 落盘前对 13 个敏感字段加密（含 event_lat/event_lng）；
  _load_tasks 加载后透明解密，内存 _tasks 保持明文（JSON 损坏仍返回 {}，字段解密失败启动 fail-fast）。
- record_agent.py：record_node 追加 events.jsonl 前对 description/address/reply/user_id/lat/lng 加密（非空才加密），
  返回结果保持明文。
- scripts/migrate_events_encryption.py（新建）：--dry-run/默认执行；enc:v1: 跳过幂等；
  执行前备份 data.bak.<时间戳>/ 且脚本结束不留残留；密钥缺失/非法退出非 0；输出每文件字段加密数摘要。
- tests/test_field_encryption.py（新建 13 用例）：helper 单测、保存/加载闭环、落盘密文+明文 grep 不可命中、
  篡改 fail-fast、record_node 加密、迁移脚本 dry-run/幂等/无残留/缺密钥、HTTP 契约对比（明文启动 vs 加密后重启响应一致）。
- scripts/verify_encryption.py：修复 ac45791 目录整理遗留的路径 bug（PROJ 指向 scripts/、缺拷 config.py）与
  概率性二进制判定（b"\x00" in blob -> 确定性非纯 ASCII 判定），使验收命令可跑且稳定全绿（非本任务功能改动）。
- README.md / deploy/DEPLOY.md：按 developer 禁区未改动（任务书范围提及加密说明，如需请另行安排）。

解密失败行为：fail-fast（与账号数据一致）——decrypt_field 对篡改/错密钥/非法编码 raise SecureStoreError，
_load_tasks 遇坏密文 raise 阻止启动，禁止静默丢数据；已有单测 + main 层测试覆盖。

B) 验收命令输出摘要
1. 新事件落盘：tasks.json/events.jsonl 敏感字段均 enc:v1:，明文关键词 grep 0 命中（test_field_encryption 断言）。
2. 迁移脚本（tests + scratch 实测）：--dry-run 不写文件；执行 19 字段加密（events 6 + tasks 13）无残留；
   二次运行 0 变更幂等；缺 DATA_ENCRYPTION_KEY exit=2。
3. 相关 pytest：test_field_encryption + chain_breaks + comprehensive + data_isolation + event_cancel
   -> 22 passed in 13.98s。
4. run_regression core：11 通过 / 0 失败 / 53.6s；full：22 通过 / 0 失败 / 163.8s；scan_secrets：0 命中。
5. verify_encryption.py：23 通过 / 0 失败；verify_cloud_integration.py --offline：PASS（临时对象已清理）。

C) 冻结契约逐条对照：全部满足（详见工作日志 docs/coordination/logs/developer-T20260821-003-2026-08-21.md）。

【请求】请按任务书验收；如 rejected，请附问题清单，我将逐条修复后发 fix-done。
