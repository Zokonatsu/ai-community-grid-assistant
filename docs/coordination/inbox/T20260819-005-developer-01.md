【消息类型】dev-ready
【任务号】T20260819-005
【版本】1
【发送方】developer-01@developer-01
【接收方】planner@01a01a02-fd78-70e2-b6fc-97a86b7d684c
【时间】2026-08-20T00:02:00+08:00
【正文】

A) 变更清单（仅 static/index.html，diff 摘要）
1. 脚本顶部新增：`let listPollTimer = null;`、`const POLL_LIST_MS = 15000;`（轮询间隔 15 秒）。
2. `loadList(quiet = false)`：新增可选参数，仅影响失败路径，渲染逻辑不变——
   - 非 2xx 且非 401：quiet 时 console.warn 后直接 return，不覆盖现有列表；非 quiet 路径保持原行为。
   - 网络异常（catch）：quiet 时 console.warn 后 return，不写「加载失败」；非 quiet 路径保持原行为（写「加载失败」）。
3. 新增 `startListPolling()`：启动前 clearInterval 防重复（单实例）；每 15 秒——replyOverlay 可见（弹窗打开）→ 跳过；document.hidden → 跳过（可选暂停）；否则 loadList(true)。
4. 底部初始化：`initAuth().then(ok => { if (ok) { loadList(); startListPolling(); } })`（加载成功后启动）。
5. `window.addEventListener('beforeunload', ...)` 卸载时清理轮询定时器。
6. 未改：statusCell/unread-badge（红点沿用 ●）、closeReplyDialog→markRead→loadList、提交轮询 pollTimer（3s）、30 秒撤销定时器 startCancelTick（与列表轮询并存）。

B) 验收命令结果
1. node --check（抽取 index.html 内联 <script>）：PASS（node v24.15.0）。
2. 行为验证：
   - 逻辑级（node 沙箱模拟 DOM/fetch/localStorage，驱动真实内联脚本）：8/8 通过——
     ① 弹窗关闭时轮询触发 loadList（events 拉取 +1）；② 弹窗打开期间轮询跳过；③ 关闭后恢复；④ document.hidden 时跳过；⑤ 非 401 失败（HTTP 500）保留现有列表（不写「加载失败」）且 console.warn；⑥ 网络异常保留现有列表；⑦ 401 走 checkUnauthorized（清 token + 跳登录）；⑧ has_new_reply=true 时 statusCell 渲染 ●。
   - 端到端浏览器（后台真实回复 → ≤15s 红点出现 → 查看关闭红点消失）需主 Agent/用户人工确认（任务书验收命令 2 的浏览器部分）。
3. 后端回归：未执行（本任务无后端改动；契约明确后端/接口/字段零改动，验收命令 3 可选）。

C) 自检结论（冻结契约逐条）
1. 轮询间隔 15 秒（POLL_LIST_MS=15000）：满足（定时器 15000ms）。
2. 触发时机：页面初始化加载成功后启动（initAuth 成功后 loadList + startListPolling）：满足。
3. 暂停：replyOverlay 可见期间不刷新，关闭后恢复；document.hidden 时暂停（可选已实现）：满足（行为验证 ②③④）。
4. 失败处理：非 401 失败/网络异常保留现有列表内容、不写「加载失败」、console 记录；401 走 checkUnauthorized 跳登录：满足（行为验证 ⑤⑥⑦）。
5. 定时器单实例：启动前 clearInterval 防重复；beforeunload 清理；与 30 秒撤销定时器并存（loadList 整体重渲染含操作列，无冲突）：满足。
6. 红点/已读：unread-badge 原样沿用（has_new_reply=true 渲染 ●，无样式改动）；closeReplyDialog→markRead→loadList 保持；轮询不干扰进行中 markRead（无需额外处理，契约注明）：满足（行为验证 ⑧）。
7. 未改 main.py/admin.html/接口/字段；不新增角标/未读数接口；不改变红点出现条件：满足（本次仅改 static/index.html）。

【请求】请按任务书验收；如 rejected，请附问题清单，我将逐条修复后发 fix-done。
