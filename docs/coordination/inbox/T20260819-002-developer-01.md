【消息类型】dev-ready
【任务号】T20260819-002
【版本】1
【发送方】developer-01@developer-01
【接收方】planner-01@01a01990-3a16-7b11-8ed4-cd5fcc48ecee
【时间】2026-08-19T20:10:00+08:00
【正文】

A) 变更清单（文件 + diff 摘要）
1. auth.py（权威校验点 register_user）：
   - 在基础校验 + 用户名/手机号查重之后、写用户之前新增：
     · 无坐标（register_lat/lng 任一为 None）→ (False, "注册需先获取定位，请允许浏览器定位权限后重试", None)
     · geo.is_within_community(register_lat, register_lng)[0] is False → (False, "当前定位不在小区范围内，无法注册", None)
   - 通过后 location_status 恒为 "verified"（删除原"越界/失败允许注册标记 unverified"逻辑）；docstring 同步。
   - 未改 geo.py / community_store.py / admin.html；不新增关闭开关。main.py 未改（schema 本就可空，错误经 AuthResponse 透传）。
2. INTERFACE.md：新增 `## HTTP API（register）` 契约段（register_lat/register_lng 业务必填 schema 可空、两类精确失败文案、半径取 geo.get_community_config() 当前生效值、成功 location_status 恒 verified、仅新注册生效）。
3. static/login.html：
   - 定位状态文案改必填语义：未定位（必须定位且需在小区范围内才能注册）；定位失败提示"未定位无法注册，请允许定位权限后重试"。
   - handleRegister：reg-lat/reg-lng 为空 → 阻止提交并在 register-error 展示"注册需先获取定位，请允许浏览器定位权限后重试"；后端 error 文案仍展示于 register-error；getPosition 6s 超时/失败降级保留。
4. tests/test_register_location.py（新增，232 行）：覆盖验收 1 无坐标拒绝+精确文案+不创建用户、2 越界拒绝+精确文案+不创建用户、3 范围内成功+verified+结构不变、4 后台改半径立即生效（旧中心点改前可注册改后拒绝）；另含 5 管理员角色注册仍被禁止。try/finally teardown 崩溃保护。
5. 存量脚本修正（保证全量回归全绿）：
   - test_comprehensive.py：7 处直接 register_user 调用补 register_lat/lng（范围内坐标 30.274150,120.155150）。
   - test_security_fixes.py：测试 4a/4d 居民注册补坐标。
   - verify_encryption.py：场景 6 补 building/unit/room + 坐标（顺带修复既有 import 缺失 bug：reset_scratch 补拷 cloud_store.py/geo.py/community_store.py，修复前脚本场景 1 即崩溃）。
   - test_auth.py / test_data_isolation.py / test_proxy_beneficiary.py / test_scene_tag.py：HTTP register 助手补坐标。
   - test_community_center.py：4.2 改断言越界注册被拒绝、4.4 改断言越界用户未创建。
   - test_resident_review_location.py：2.x 改断言越界注册被拒绝、5.2 改断言 res_out 未创建。

B) 测试结果
1. python tests/test_register_location.py（C:\Users\78397\Desktop\ai-community-grid-assistant\.venv\Scripts\python.exe）：19/19 通过。
2. 全量回归（tests/test_*.py 逐个串行，PYTHONIOENCODING=utf-8，每跑后检查无残留备份目录）：13/13 全绿——
   test_auth 40/40、test_cloud_store、test_community_center、test_comprehensive、test_data_isolation、test_geo、test_input_validation（UTF-8 无崩溃）、test_life_rescue_fix、test_proxy_beneficiary 14/14、test_resident_review_location 22/22、test_scene_tag 29/29、test_security_fixes、test_semantic_timeout 30/30。
   test_server.py 未跑（远程部署主机冒烟，非本地回归，同 T20260819-001）。
3. verify_encryption.py：22/23 通过（场景 6 注册→重启→登录 PASS）。唯一 FAIL「加密文件是二进制(含0字节)」为既有概率性检查（AES-GCM 随机 nonce 下密文是否恰含 0x00 字节），与本次变更无因果关系。
4. 前端 JS：抽取 login.html 内联 <script> → node --check PASS。

C) 自检结论（冻结契约逐条）
1. 无坐标（任一 null）→ success=false + 精确文案「注册需先获取定位，请允许浏览器定位权限后重试」，不创建用户：满足（HTTP 与 auth.register_user 域逻辑均验证）。
2. 越界（> radius_m）→ success=false + 精确文案「当前定位不在小区范围内，无法注册」，不创建用户：满足。
3. 范围内（≤ radius_m）→ 成功，data.user.location_status 恒为 "verified"，其余响应结构不变：满足。
4. 半径读取 geo.get_community_config() 当前生效值（data/community_config.json 持久化优先，env 兜底 500m），后台改半径立即生效无需重启：满足（test 4.x 验证）。
5. 限制强制开启、无关闭开关；HTTP 路径与域逻辑均执行校验：满足（main.py 未动，校验全部落在 register_user）。
6. 仅新注册生效；存量用户数据/登录/list_users 后台逻辑未改：满足（存量用户记录零改动，回归含 list_users/登录全绿）。
7. 前端未定位阻止提交 + 后端错误文案展示于 register-error（不跳转）：满足。
8. 管理员角色注册仍被禁止：满足（test 5.1）。
9. 数据完整性：测试后 data/secure 恢复原状（users.json.enc 555B 与测试前一致）；解密核对 secure/users.json.enc 仍含 admin 账号（role=admin，哈希格式正常），未打印密钥/哈希；无残留 *.bak.* 目录；手工备份 _dev01_backup_20260819 保留待验收。
