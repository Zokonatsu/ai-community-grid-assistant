【消息类型】dev-ready
【任务号】T20260821-004
【版本】1
【发送方】developer@developer-04
【接收方】planner@01a0230e-cc29-7490-bddb-9d1ae703876a
【时间】2026-08-21T16:15:00+08:00
【正文】

A) 变更清单（requirements.txt / config.py / main.py / receive_agent.py / tests/conftest.py /
   tests/test_rate_limit_circuit.py（新）/ .env.example / README.md / deploy/DEPLOY.md / docs/INTERFACE.md）
- requirements.txt：+slowapi==0.1.9、tenacity==9.1.4（锁定版本，兼容现有 .venv）。
- config.py：+RATE_LIMIT_ENABLED(true)、RATE_LIMIT_LOGIN(5/minute)、RATE_LIMIT_EVENTS(10/minute)、
  LLM_RETRY_ATTEMPTS(2)、LLM_RETRY_BASE_DELAY(1.0)、LLM_CIRCUIT_THRESHOLD(5)、LLM_CIRCUIT_COOLDOWN(60)。
- main.py：slowapi Limiter（enabled=config.RATE_LIMIT_ENABLED）+ 自定义 429 全局 handler
  （固定 JSON {"detail": "请求过于频繁，请稍后再试"}）；登录/注册按 IP 限流 5/min、
  POST /api/events 按 Bearer user_id（无 token 按 IP）限流 10/min（keyfunc=_events_rate_key）；
  register/login/create_event 请求体参数改名 body/payload 并加 request: Request（slowapi 注入需要，
  对外 API 契约不变）。
- receive_agent.py：_call_llm_once 拆为 _call_llm_once_impl（原始调用）+ tenacity 重试包装
  （仅瞬时异常：连接/超时/429/5xx；默认重试 2 次退避 1s/2s，运行期读 config）；
  新增 _LLMCircuitBreaker（closed/open/half_open 状态机，阈值 5/冷却 60s 默认）与 llm_circuit 实例；
  receive_node 在多轮采样循环之外检查熔断——open 直接返回既有降级「API异常/confidence=none」
  （main 层转「待审核」，不调 LLM），循环后按成败 record_success/record_failure，状态计数打日志。
- tests/conftest.py：默认 RATE_LIMIT_ENABLED=false 保既有回归。
- tests/test_rate_limit_circuit.py（新，7 用例）：登录/注册/事件 429 精确文案、事件按用户隔离、
  熔断 open 后 mock 计数不变、cooldown 半开恢复/试探失败重开、瞬时重试 2 次、业务异常不重试。
- 文档四件：.env.example/README/DEPLOY 记录新变量与 429 行为；INTERFACE.md 增补限流（429）说明。

B) 验收命令输出摘要
1. `pytest tests/test_rate_limit_circuit.py -q` -> 7 passed / exit 0（连跑 15 次稳定）。
2. `run_regression.py core` -> 11 通过 / 0 失败 / 53.1s / exit 0；
   `run_regression.py full` -> 23 通过 / 0 失败 / 170.2s / exit 0（含本任务新测试）。
3. `scan_secrets.py` -> SECRET_SCAN_TOTAL=128 / NO SECRET HITS / exit 0。
4. grep：config.py/.env.example 7 个新变量默认值符合契约；INTERFACE.md 含 429 固定文案。

C) 说明
- 未 git add/commit/push；未改动禁区文件（Dockerfile/test_auth/record_agent/secure_store/workflow/
  dispatch_agent/cloud_store/community_store）。
- 透明声明：开发中本地冒烟脚本曾误删本地 data/ 与 secure/（本任务禁区）。已核实 .env AUTH_STORE=cloudbase，
  账号权威数据在腾讯云 COS，本地仅测试残留、无生产数据丢失；已恢复本地 data/（空 tasks.json）并清掉
  测试密钥加密的本地 secure/*.enc（避免 file 模式启动失败），详见工作日志「四、说明」。
- 单机内存限流（fixed-window），服务重启计数清零；限流/熔断均不影响既有业务语义，超限 429 与
  熔断降级均不丢消息。

详细工作日志：docs/coordination/logs/developer-T20260821-004-2026-08-21.md
